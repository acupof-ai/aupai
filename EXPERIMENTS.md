# Experiments

Auto-generated by `scripts/exp.py` — every GPU run is recorded here.

53 runs, 40 completed. Newest first.

| started | name | status | result | notes | commit |
|---|---|---|---|---|---|
| 2026-08-29 16:51 | k8_proc_replay | ok | probe_procedure BOTH 0.0/0.0/0.0, STEPS 5/180 (ARM A 4/180); digit head teacher-forced 61.1% (600/982) vs ARM A 57.2% (562/982), McNemar chi2=15.9 p=6.6e-05 |  | 5c7de1e |
| 2026-08-29 16:41 | k8_v3_fone | ok | 3.29B tok, val 2.988, probe_procedure BOTH 0.0/0.0/0.0 (STEPS 0 on all three) | 3.29B tok, val 2.988, 73K tok/s/gpu, 6597s on 7xH20 | 03cd080 |
| 2026-08-29 16:41 | k8_proc_sft | ok | probe_procedure BOTH 0.0/0.0/0.0; STEPS 0/180 -> 4/180; digit head teacher-forced 21.3% -> 57.2% (209 -> 562 of 982) |  | 03cd080 |
| 2026-08-29 06:15 | k8_v3_fone | ok | math-hard 0/1032 = 0.0% \| math-500 11/500 = 2.2% \| val 2.988 \| 3.29B tok | corpus v3 + --fone, 7 GPUs (GPU7 reserved for tileRL), vocab 0bce3584bc24f255 copied to data/tokenizer_k8.json |  |
| 2026-08-28 18:00 | sft_k5_v11 | ok | TOTAL math-500: 226/500 = 45.2% \| TOTAL math-hard: 34/1032 = 3.3% | 4129 rows x 4097 tok = 16.9M tokens |  |
| 2026-08-28 17:35 | sft_k5_v9_ep2 | ok | TOTAL math-500: 256/500 = 51.2% \| TOTAL math-hard: 28/1032 = 2.7% | 4064 rows x 4097 tok = 16.7M tokens |  |
| 2026-08-28 17:24 | fone_arith | ok | wrong-equation rate: k6 base 32.7% vs k5 43.3% (z=-7.1, p~1e-12); after the same SFT, k6 30.2% vs k5 37.7% (z=-5.6, p~2e-8). k6 also emits MORE equations, 77% of generations against 66%, so it is not avoiding arithmetic. | k6 base 32.7% vs k5 43.3% (4722 / 3937 verifiable equations, deduplicated n) |  |
| 2026-08-28 17:14 | sft_k5_v10 | ok | TOTAL math-500: 221/500 = 44.2% \| TOTAL math-hard: 27/1032 = 2.6% | 2480 rows x 4097 tok = 10.2M tokens |  |
| 2026-08-28 16:00 | sft_k5_v9 | ok | TOTAL math-500: 222/500 = 44.4% \| TOTAL math-hard: 41/1032 = 4.0% | 4064 rows x 4097 tok = 16.7M tokens |  |
| 2026-08-28 15:40 | sft_k6_mix6 | ok | math-hard 3.2% (33/1032), math-500 47.0% |  |  |
| 2026-08-28 15:40 | sft_k5_ctrl | ok | math-hard 3.6% (37/1032), math-500 44.0% -- best math-hard on record (previous 3.1%) |  |  |
| 2026-08-28 15:06 | sft_k5_ctrl | ok | TOTAL math-500: 220/500 = 44.0% \| TOTAL math-hard: 37/1032 = 3.6% | 4571 rows x 4097 tok = 18.7M tokens |  |
| 2026-08-28 14:59 | sft_k5_mix6 | killed | NOT RECORDED | 4658 rows x 4097 tok = 19.1M tokens |  |
| 2026-08-28 14:23 | sft_k6_mix6 | ok | TOTAL math-500: 235/500 = 47.0% \| TOTAL math-hard: 33/1032 = 3.2% | 4834 rows x 4097 tok = 19.8M tokens |  |
| 2026-08-28 13:58 | k6_base_eval | ok | math-hard 1.7% (18/1032) vs k5 1.9% (z=-0.33, p=0.743) and k4 2.9% (z=-1.75, p=0.080); MMLU 24.5% vs k5 25.8%, both at the 25% chance line | k6 math-hard 1.7% (18/1032), MMLU 24.5%; k5 1.9% / 25.8%; k4 2.9% |  |
| 2026-08-28 13:58 | sft_k6_mix | ok | TOTAL math-500: 2/500 = 0.4% \| TOTAL math-hard: 19/1032 = 1.8% | 4834 rows x 4097 tok = 19.8M tokens |  |
| 2026-08-28 10:14 | sft_k5 | ok | math-500 44.8% (224/500) \| math-hard 1.3% (13/1032; L3 1%, L4 1%) | 12934 rows x 4097 tok = 53.0M tokens; 33 steps, loss 1.489 -> 1.200 | 96901e0 |
| 2026-08-28 09:03 | k5_clean_0827 | ok | math-hard 1.9% (20/1032; L3 3%, L4 1%) \| val 2.020 \| 11.48B tok, 5.5h on 7xH20 |  | 4d05f89 |
| 2026-08-28 07:03 | k6_fone | ok | math-hard 1.7% (ckpt_k6_fone) | 11.33B tok scheduled; rebuilt stratified vocab (12.5% fewer tokens); 930 tok/doc vs 1047; 148.3M [NUM] in web; 74K tok/s/gpu vs k5's 85K (-14%, float64 digit extraction + digit CE) |  |
| 2026-08-28 02:58 | k5_clean_0827 | ok | math-hard 1.9% (20/1032; L3 3%, L4 1%) \| val 2.020 \| 11.48B tok, 5.5h on 7xH20 | mix.json schedule: web 0.88/en 0.05/math 0.04/code 0.02/chat 0.01, anneal math 0.28 at anneal_frac 0.10 (same anneal shape as k4). 7 GPUs (GPU7 occupied at launch). | 4d05f89 |
| 2026-08-28 01:00 | sft_k5 | fail | eval failed (math-500 rc=0, math-hard rc=1) | 12934 rows x 4097 tok = 53.0M tokens |  |
| 2026-08-27 13:16 | k5_clean_0827 | ok | val 2.020; math-500 51.2%; math-hard 1.9% |  |  |
| 2026-08-27 10:46 | rl_k4 | ok | math-500 44.6% \| math-hard 4.1% (42/1032) \| RL-set acc 0.489 -> 0.653 |  | 7a2ac5b |
| 2026-08-27 10:46 | k4_11b_lr05 | ok | math-500 51.6% \| math-hard 2.9% \| val 2.086 |  | 7a2ac5b |
| 2026-08-27 06:14 | sft_k4 | ok | math-500 39.2% (196/500); math-hard deferred to the bench stage |  | 7a2ac5b |
| 2026-08-27 03:45 | rl_direct | ok | math-500 52.4% (262/500) \| math-hard 2.9% (30/1032) \| pass@8 7.0% \| RL-set acc 0.342->0.645 (peak 0.668@280, dip 0.339@390) | GSPO, group 8, T=0.9, KL 0.02, prompts filtered to the 20-80% solve-rate band |  |
| 2026-08-27 00:21 | throughput_bisect | ok | no regression; batch 32 without grad_ckpt = 90K tok/s/gpu MFU 38%, batch 72 with grad_ckpt = 37K MFU 15% | 8xH20, 40 steps per arm, 13 arms | e066891 |
| 2026-08-26 23:17 | rl_k4 | ok | rc=0 \| step 500/500 acc 0.635 loss 0.0013 gnorm 10.856 gen 37s degen 13 | GSPO, group 8, T=0.9, KL 0.02, prompts filtered to the 20-80% solve-rate band |  |
| 2026-08-26 23:16 | rl_k4 | fail | rc=1 \| step 90/500 acc 0.016 loss 0.0000 gnorm 0.350 gen 81s degen 23 | GSPO, group 8, T=0.9, KL 0.02, prompts filtered to the 20-80% solve-rate band |  |
| 2026-08-26 22:04 | doc_mask_ab | ok | no_doc_mask 9m04s / docmask 8m18s, both exit 0, both 32 recompiles | 2xH20, same seed, 6 steps + epoch-end validation | cf8adb1 |
| 2026-08-26 22:03 | sft_k4 | fail | eval failed (math-500 rc=0, math-hard rc=1) | 12934 rows x 4097 tok = 53.0M tokens |  |
| 2026-08-26 21:56 | sft_k4 | fail | train exited 1 | 12934 rows x 4097 tok = 53.0M tokens |  |
| 2026-08-26 20:55 | ab2_attnres_vs_base | fail | both arms died at 2m43s: CUDA OOM, 94.9/95.2 GiB on every rank | 8xH20, batch 72, flat 2.26B token cache, same seed, 500 steps each = 1.18B tokens/arm | 2c7ccf4 |
| 2026-08-26 20:36 | rlvr_gspo | stopped | stopped at step 90/500: acc 1.6-2.3%, gnorm 0.35-0.43, ~30% of groups degenerate |  | 2c7ccf4 |
| 2026-08-26 19:34 | sft_v5_hard | ok | math-hard: 32/1032 = 3.1% (L3 4%, L4 3%) |  | e3bb5c4 |
| 2026-08-26 17:39 | rlvr_stage2 | fail | math-500 11.2% COLLAPSED, ckpt deleted | GRPO, no KL, T=0.1, 218K problems | 3afceec |
| 2026-08-26 17:39 | sft_v2_stage1 | fail | math-500 26.4% (worse: lost openo1/school_math_r1 volume) | 350K short mix from pretrain, 64M tokens | 3afceec |
| 2026-08-26 17:38 | pretrain_k3_v2 | ok | pretrain ckpt_k3-mla_2b_step2000.pt | K3 KDA+MLA 206M, 2.26B tokens, FP8, 3 epochs | 3afceec |
| 2026-08-26 17:38 | sft_stage1 | ok | math-500 32.2% (baseline) | orig mix: alpaca+coig+openo1+gsm8k_zh+school_math_r1+s1k | 3afceec |
| 2026-08-26 17:38 | sft_math_stage2 | ok | math-500 32.2% | stage-2 on school_math+gsm8k+alpaca+coig | 3afceec |
| 2026-08-26 17:38 | sft_short_stage2 | ok | math-500 31.6% (format alone no gain) | short-format ablation, 12.4K rows | 3afceec |
| 2026-08-26 16:53 | k4_11b_lr05 | ok | ep 1/1 train 2.005 val 2.086 18015s | 8xH20 batch 24 no grad_ckpt, FP8, doc_mask, AttnRes Block-4 (gain folded), lr_scale 0.5; mix 11.5B tokens, anneal math 4.0->28.3%; 14606 steps |  |
| 2026-08-26 16:26 | k4_11b_0827 | ok | math-hard 2.9% (ckpt_k4_11b_lr05) | 8xH20 batch 24 no grad_ckpt, FP8 (torchao), doc_mask on, AttnRes Block-4 with the gain folded into the query; mix 11.5B tokens over web 8.31B / en 0.20B / math 0.16B / code 0.08B / chat 0.04B, anneal last 10% with math 4.0% -> 28.3%; 14606 steps at 82K tok/s/gpu = ~4.9h |  |
| 2026-08-26 12:07 | rlvr_gspo2 | ok | stopped at ~step 45/500: acc 1.4-2.2%, gnorm 0.31-0.42, 55% of groups degenerate | GSPO seq-level ratio+clip, k3 KL anchor to the frozen SFT ref, degenerate-group filtering, T=0.9; base ckpt_sft_v5 (math-500 31.0%, math-hard 3.1%) |  |
| 2026-08-26 12:04 | rlvr_gspo | fail | aborted: duplicate launch shared port 29540 and one log file | GSPO seq-level ratio+clip, KL anchor to frozen SFT ref, degenerate-group filtering, T=0.9; base ckpt_sft_v5 (math-500 31.0%, math-hard 3.1%) |  |
| 2026-08-26 12:01 | rlvr_gspo | killed | NOT RECORDED | GSPO seq-level ratio+clip, KL anchor to frozen SFT ref, degenerate-group filtering, T=0.9; base ckpt_sft_v5 (math-500 31.0%, math-hard 3.1%) |  |
| 2026-08-26 11:39 | ablation_attnres | killed | NOT RECORDED | AttnRes A/B at equal tokens, torchao FP8, 6xH20 |  |
| 2026-08-26 11:30 | sft_v5 | ok | math-500: 155/500 = 31.0% |  |  |
| 2026-08-26 10:58 | sft_v5 | ok | TOTAL math-500: 155/500 = 31.0% | 55243 rows x 4097 tok = 226.3M tokens |  |
| 2026-08-26 10:54 | sft_v5 | fail | train exited 1 | 55243 rows x 4097 tok = 226.3M tokens |  |
| 2026-08-26 10:13 | sft_v4 | ok | TOTAL math-500: 160/500 = 32.0% | 54869 rows x 4097 tok = 224.8M tokens |  |
| 2026-08-26 09:49 | sft_v3 | ok | TOTAL math-500: 134/500 = 26.8% | 17668 rows x 4097 tok = 72.4M tokens |  |
| 2026-08-26 09:39 | perf_sweep | ok | batch 32=29s/10步, 64=57s/10步 -> 吞吐恒定271K tok/s (compute-bound); no_grad_ckpt NaN -> 根因见 850369a | throughput: grad_ckpt on/off x batch |  |

## What each run taught us

### k8_proc_replay — probe_procedure BOTH 0.0/0.0/0.0, STEPS 5/180 (ARM A 4/180); digit head teacher-forced 61.1% (600/982) vs ARM A 57.2% (562/982), McNemar chi2=15.9 p=6.6e-05
- **Asked:** ARM B of the pre-registered experiment: same procedure SFT plus 7.4% general replay (knowledge_qa_zh 1010 + code_python_zh 3000). Forgetting is measured twice on this project (math-500 51.2->44.8 p=0.043; 51.6->39.2 p<0.001) and replay had never been tried. Risk neither prior covers: sub-500M full FT overwrites priors, so replay might DILUTE the procedure signal.
- **Learned:** Replay does not dilute -- it significantly IMPROVES the procedure signal, the opposite of the one risk neither the Flan-T5-250M nor the Fine-Tuning-Trap prior covered. Both arms leave BOTH at 0: the rollout failure is untouched by replay, as expected, because replay addresses forgetting and not exposure bias.
- **So:** Replay is the default for every future SFT on this project at no measured cost and a measured gain. It does NOT address the rollout failure; self-generated-prefix training is the next arm.

### k8_v3_fone — 3.29B tok, val 2.988, probe_procedure BOTH 0.0/0.0/0.0 (STEPS 0 on all three)
- **Asked:** First combination of corpus v3 (filtered, 3.3B) with FoNE. Every FoNE conclusion so far rests on a v2 base measured against a v2 control.
- **Learned:** First corpus-v3 + FoNE base. Procedure execution is absent at base as expected -- nothing in the corpus contains executed procedures. Digit head teacher-forced 21.3% (209/982) on gold procedure text.
- **So:** Baseline for the pre-registered procedure-SFT experiment (docs/exp_procedure_sft.md). math-hard not yet run.

### k8_proc_sft — probe_procedure BOTH 0.0/0.0/0.0; STEPS 0/180 -> 4/180; digit head teacher-forced 21.3% -> 57.2% (209 -> 562 of 982)
- **Asked:** Pre-registered docs/exp_procedure_sft.md: procedure execution is absent because no SFT run ever used data containing EXECUTED procedures. procedure_v1 is the first that does (answer-last, every line machine-verified, one format per prompt). Gate is probe_procedure BOTH on held-out problems, NOT math-hard. Control measured: bare k8 = BOTH 0.0/0.0/0.0, STEPS 0 on all three procedures.
- **Learned:** THE PRE-REGISTERED READING WAS WRONG and the pre-registration is how I know. 'BOTH near zero -> coverage was not the constraint' does not fit: teacher-forced, the model predicts 57.2% of the numbers in gold procedure text against 21.3% at base, and it learned the unit-conversion chain shape and the terminator. It cannot survive its own rollout -- one wrong number early and the chain degenerates ('1/1 = 1/1' loops). That is exposure bias, not missing coverage. SFT loss fell to 0.144 with zero generalisation: it fit next-token-given-gold-prefix, not procedure execution.
- **So:** A fourth branch the pre-registration lacked. 57.2% teacher-forced CROSSES lessons-b0's own >50% step-correctness gate, so its conditional objection to step-level RL no longer binds. Next: scheduled sampling / self-generated-prefix SFT before any RL, since the failure is recovery from own error.

### k8_v3_fone — math-hard 0/1032 = 0.0% | math-500 11/500 = 2.2% | val 2.988 | 3.29B tok
- **Asked:** FoNE was measured only on the v2 unfiltered corpus. Corpus v3 and FoNE have never been combined; does the representation move math-hard on filtered data?
- **Learned:** The base is at ZERO on math-hard and 2.2% on math-500 against k5's 1.9%/51.2%. The eval is correctly configured -- math_hard.py auto-detects cfg.fone and threads the [NUM] value channel, and k6_fone scores 1.7% through the same path -- so this is a real gap, not the documented value-channel failure. THREE candidate causes and no measurement separating them: 3.5x fewer tokens (3.29B vs 11.5B), corpus v3 removing the contamination that inflated k5's math-500, and a different mix. ckpt_k7_v3 is the control that separates FoNE from corpus and is running.
- **So:** Do not read this as 'FoNE on v3 fails' until k7_v3 lands. Corrects the earlier reading that procedure SFT destroyed math ability: the base was already at zero, so k8_proc's 0.0% is not a collapse.

### fone_arith — wrong-equation rate: k6 base 32.7% vs k5 43.3% (z=-7.1, p~1e-12); after the same SFT, k6 30.2% vs k5 37.7% (z=-5.6, p~2e-8). k6 also emits MORE equations, 77% of generations against 66%, so it is not avoiding arithmetic.
- **Asked:** FoNE was scored only on end-to-end solving, where it was null. Its actual claim is narrower: numbers written as [NUM] with a Fourier value should be COMPUTED more accurately. That is measurable separately, in the arithmetic of the generated steps.
- **Learned:** FoNE does exactly what it was designed to do, and it is not the constraint. A 25% relative cut in arithmetic errors, the largest effect measured today by orders of magnitude, moves end-to-end math-hard not at all (3.2% vs 3.5%, p=0.627). Reading the generations says why: failures are setup collapse and template misapplication, not slips in arithmetic -- the model reaches for 效率 = 1 / n on train and boat problems where it means nothing.
- **So:** Keep --fone off by default; the 14% throughput cost still buys no score. But the earlier entry saying it 'buys nothing' was incomplete and is corrected here. Wrong-equation rate is now part of scripts/eval_all.sh: score and arithmetic are different questions, and today the second one moved while the first did not.

### sft_k6_mix6 — math-hard 3.2% (33/1032), math-500 47.0%
- **Learned:** The best FoNE arm. Its own base was 1.7%, so the SFT is worth +1.5pt (z=+2.13, p=0.033) -- but the non-FoNE control on the same data reached 3.6%, so none of that is attributable to the number representation.
- **So:** Superseded by sft_k5_ctrl as the reference number.

### sft_k5_ctrl — math-hard 3.6% (37/1032), math-500 44.0% -- best math-hard on record (previous 3.1%)
- **Learned:** FoNE BUYS NOTHING. Same data, same 6 epochs, same 24 batch: the plain k5 base reaches 3.6% and the FoNE k6 base reaches 3.2%, a net -0.4pt (z=-0.49, p=0.627). Both beat their own base significantly (k5 1.9->3.6, p=0.022; k6 1.7->3.2, p=0.033), so the whole gain is the SFT data. The mechanism did work -- k6's digit head hits 66.5% whole-number exact on held-out math against a 16.4% copy-previous baseline -- which makes the finding sharper: BPE number segmentation is real, fixable, and NOT the constraint on math-hard.
- **So:** Drop --fone: it costs 14% throughput (73K vs 85K tok/s/gpu) for no measurable return. Keep the code behind the flag, since the digit-accuracy result is worth re-testing on a base that can actually solve these problems. The lever that moved is data: Belle removed at 38.7% hand-measured defect rate, plus verified synthetic and contamination scrubbing, took both bases from ~1.8% to ~3.4%.

### sft_k5_mix6 — NOT RECORDED
- **Learned:** ckpt_sft_k5_mix6 exists on record with no math-hard. This is the nohup-vs-setsid death: the checkpoint saved, the eval stage never ran.
- **So:** Closed as killed 2026-08-29. Score it or drop the checkpoint; harness gaps lists it either way.

### k6_base_eval — math-hard 1.7% (18/1032) vs k5 1.9% (z=-0.33, p=0.743) and k4 2.9% (z=-1.75, p=0.080); MMLU 24.5% vs k5 25.8%, both at the 25% chance line
- **Asked:** If the BPE number segmentation was the math-hard ceiling, a FoNE base beats k5 by much more than the metric's +/-1.1pt resolution.
- **Learned:** FALSIFIED at the base-model level. The digit head plainly learned -- 66.5% whole-number exact on held-out math against a 16.4% copy-previous baseline -- and none of it reached end-to-end solving. Every pairwise difference sits inside the metric's resolution. MMLU cannot discriminate here: both checkpoints are at chance, and they use different vocabularies, so its 1.3pt gap is not attributable.
- **So:** Two readings remain: the base cannot solve these problems at 2-3% either way, so number representation is not yet the binding constraint; or the constraint is multi-step reasoning rather than arithmetic. SFT separates them -- if k6 still matches k5 after SFT, FoNE does not pay for its 14% throughput.

### sft_k5 — math-500 44.8% (224/500) | math-hard 1.3% (13/1032; L3 1%, L4 1%)
- **Asked:** Same SFT data (sft_k4.pt) on the clean base instead of k4's: isolates the pretrain corpus as the only variable in the SFT stage.
- **Learned:** SFT hurts, and this is now a two-line result rather than a one-off. Against its own base the drop is significant on math-500: k5 51.2% -> 44.8%, -6.4pt, z=-2.03, p=0.043; the k4 line did the same thing harder, 51.6% -> 39.2%, -12.4pt, p<0.001. Same SFT data, two different bases, same direction. On math-hard the within-line drop (1.9% -> 1.3%) is NOT significant (p=0.219) -- that metric cannot resolve it. The same eval sweep also filled in the missing cell that makes the base comparison decidable: k5 base math-500 is 51.2% against k4 base's 51.6% (p=0.899), so the two bases are indistinguishable on BOTH math metrics while k5 holds a better val (2.020 vs 2.086). The clean corpus buys better language modelling at equal math ability -- it is not a math regression, which was the earlier worry. On WHY the SFT data hurts, one property is measured and solid: its answers run a 156-char median and 377-char p90, roughly twice the base corpus (73-89) and twice the eval's own reference answers (85). EXPERIMENTS.md records that SFT's useful active ingredient was length COMPRESSION (40-char median vs the base's 104) -- this dataset does the opposite. That is consistent with the observed damage but is not a causal proof. Four other defects were hypothesised from probe output and every one turned out to be an artifact of the probe, not the data: missing EOS separators (present, id=1, and carrying loss on 6701 of 21511), 34 questions concatenated per row (normal EOS-delimited packing, and sft.py doc-masks across them anyway), 21% of answers ending on a mismatched \\boxed (LaTeX display plus the 答案是： restatement, values agree), and 4.3% label noise from boxed-copied-from-question (the heuristic's own printed examples were false positives).
- **So:** Do not run SFT with this dataset again. The next experiment is the one EXPERIMENTS.md already named and this run reinforces: length-only SFT -- compress base generations to ~40-char median with no math content -- then RL, to test length compression as the active ingredient in isolation. Method note, the expensive lesson of this session: verify the probe before believing the finding. Six of the day's conclusions came from probe artifacts (four here, plus a broken arithmetic checker and a decode that stripped the EOS being looked for), and separately five causal hypotheses were raised for a k5-vs-k4 gap that a significance test would have shown did not exist. Test significance first, then validate the instrument, then hypothesise.

### k5_clean_0827 — math-hard 1.9% (20/1032; L3 3%, L4 1%) | val 2.020 | 11.48B tok, 5.5h on 7xH20
- **Learned:** The clean corpus improves language modelling (val 2.020 vs k4's 2.086) and does NOT hurt math: the math-hard gap to k4 (1.94% vs 2.91%, 20 vs 30 of 1032) is NOT significant -- two-proportion z=1.43, p=0.152, with 95% CIs [1.10,2.78] and [1.88,3.93] overlapping across most of their range. That significance test should have been the FIRST step; instead five causal hypotheses were raised and all five were falsified by measurement: (1) missing math anneal -- false, math.anneal=0.28 at anneal_frac=0.10, the same shape as k4; (2) format mismatch -- false, clean math is 100% the eval's 问：/答：\\boxed{} shape; (3) k4 winning on eval leakage -- false, the holdout filter flags 0/5000 on every math source, clean and k4-era alike; (4) shallower solutions -- false, and backwards: k5's v6/v7 run a 73-char median answer at 67% L3+L4 against k4's v2/v4 at 58-64 chars and 50%; (5) fraction-vs-decimal answer formatting -- false, the grader's to_number parses a/b and compares numerically at 1e-4 tolerance. The real lesson is about the metric, not the model: at a ~2-3% pass rate over 1032 problems, math-hard cannot separate two base checkpoints -- its resolution is roughly +-1pt, the same size as the effect being measured. One genuine data defect surfaced along the way and is worth fixing on its own terms: 2052 ape210k rows (3.8% of that source, 1.0% of the math corpus) keep an intact question but have the answer corrupted to the placeholder 列式：1 = 1 -> \\boxed{1}.
- **So:** Accept the clean corpus: val improved, math-hard is unchanged within noise, so nothing about this run argues for reverting. Two follow-ups, in order. (1) Fix the metric before trusting any further base-vs-base math comparison -- at 2-3% pass rate math-hard resolves to about +-1pt, so either report pass@k (which spreads the signal) or size the eval set to the effect you need to detect; base-stage comparisons should lean on val until then. (2) Drop or repair the 2052 placeholder-answer ape210k rows, independent of this comparison. Process note for future runs: compute the significance of a delta before hypothesising a cause for it.

### k6_fone — math-hard 1.7% (ckpt_k6_fone)
- **Asked:** The ~3% math-hard ceiling is a number-representation failure, not a capacity or corpus failure: BPE segments numbers by corpus frequency rather than place value (1640 -> 16|40), so a carry rule learned on one number cannot transfer. A GPU A/B at equal params and steps gave BPE 1.4% and FoNE 100% on held-out two-digit arithmetic. If that transfers, k6 beats k5 on math-hard by much more than the +/-1pt resolution of the metric.
- **Learned:** FoNE fixes number representation (digit head 66.5% vs 16.4% baseline; wrong-equation rate 43.3->32.7%, p~1e-12) and moves math-hard not at all. A second constraint sits downstream.
- **So:** Row closed retroactively 2026-08-29. FoNE carried into k8_v3_fone, its first combination with corpus v3.

### k5_clean_0827 — math-hard 1.9% (20/1032; L3 3%, L4 1%) | val 2.020 | 11.48B tok, 5.5h on 7xH20
- **Asked:** A corpus rebuilt end-to-end through build_corpus.py (clean/dedup/holdout-filter every domain) beats k4's older corpus at equal scale (~11.5B tokens), same architecture and recipe.
- **Learned:** The clean corpus wins at language modelling and LOSES at math: val 2.020 beats k4's 2.086, but math-hard falls 2.9% -> 1.9%. Three explanations were tested and all falsified: the anneal is NOT missing (math.anneal=0.28 at anneal_frac=0.10, the same shape as k4); the format is NOT mismatched (clean math is 100% the eval's 问：/答：\\boxed{} shape); and k4 was NOT winning on eval leakage (holdout flags 0/5000 on every math source, clean and k4-era alike). What the data does show is a solution-DEPTH mismatch: 65% of the math corpus is math_short_v6/v7 at an 89-char median answer -- three-line template solutions (超过=6-3 / 加价=3x3 / 车费=10+9) -- and another 21% is ape210k at 45 chars, against the multi-step chains math-hard L3/L4 requires. The corpus teaches short-form routine and the benchmark tests long-form reasoning, which is exactly the failure seen when tasting the checkpoint: fluent, correctly-formatted three-line answers with wrong logic. A separate real defect was found and is worth fixing on its own: 2052 ape210k rows (3.8% of that source, 1.0% of the math corpus) have intact questions but answers corrupted to the placeholder 列式：1 = 1 -> \\boxed{1} -- 1% is too small to explain the gap but it is actively teaching the model to emit \\boxed{1} on sight of a word problem.
- **So:** Do NOT retrain yet, and do not conclude the clean corpus is a regression -- val says the opposite. Two fixes first: (1) drop or repair the 2052 placeholder-answer ape210k rows; (2) raise the math corpus's solution depth to the eval's scale via mathbank multi-length solution generation (already the planned next math batch). The one quantity still unmeasured is the solution depth of k4's own 435K anneal examples -- measure it on the pod before designing the next mix, since it is the direct control for this hypothesis.

### k5_clean_0827 — val 2.020; math-500 51.2%; math-hard 1.9%
- **Asked:** fresh clean-corpus pretrain from scratch: 9.49B tok tokenized (web 9.01B + small domains), best recipe, GPU 0-6
- **Learned:** Indistinguishable from k4 on both math metrics (math-500 51.2 vs 51.6, p=0.899; math-hard 1.9 vs 2.9, p=0.152) while holding a better val (2.020 vs 2.086). The clean corpus buys language modelling at equal math.
- **So:** Kept as the k5 base. The math ceiling is not a corpus problem, so the next move is the number representation (k6_fone), not more cleaning.

### rl_k4 — math-500 44.6% | math-hard 4.1% (42/1032) | RL-set acc 0.489 -> 0.653
- **Learned:** RL works, and the difficulty filter is why. Restricting prompts to the 20-80% solve-rate band cut degenerate groups from 30-55% to 3% at the start, and accuracy on the RL set rose 0.489 -> 0.653 over 500 steps; it transferred, taking math-hard 3.2% -> 4.1%, the first time that metric has moved off ~3% across four SFT runs and three RL attempts. But RL could not repay what SFT had cost: 44.6% on math-500 against the base's 51.6%.
- **So:** Drop the SFT stage and run RL directly on the pretrain base. SFT is now measurably harmful here -- see the sft_k4 record -- and the base has both the higher starting point (51.6% / 2.9%) and the wider pass@8 gap (+10.1% vs +5.4%), which is the quantity RL amplifies.

### k4_11b_lr05 — math-500 51.6% | math-hard 2.9% | val 2.086
- **Learned:** The anneal did the SFT stage's job, better than SFT did. 28.3% math in the last 10% of tokens -- 435K examples in the same 问：/答：\\boxed{} shape the eval uses -- left the raw base at 51.6% on math-500, against 31.0% for the old base AFTER supervised fine-tuning. Format came with it: 91.6% of the base's math-hard generations are answer-parseable, higher than the 88.2% after SFT. What the anneal did not buy is hard-problem ability: math-hard is 2.9%, the same ~3% the old pipeline reached.
- **So:** This checkpoint, not the SFT one, is the base for everything downstream.

### sft_k4 — math-500 39.2% (196/500); math-hard deferred to the bench stage
- **Learned:** First break from the 32% plateau: four SFT runs on the 2.26B-token base landed at 32.2 / 26.8 / 32.0 / 31.0, and this one is 39.2% off an 11.5B-token base. The number is also more trustworthy than those four, which were measured against a holdout that had 496 of its 500 questions sitting in the training corpus with full solutions -- that leak was closed today, so 39.2% is a measurement where 32% was partly recall.
- **So:** math-hard against the old base's 3.1% is still the number that decides whether the pretrain was worth it; math-500 is saturated by design and CLAUDE.md already says not to conclude from it. Proceed to the solve-rate probe.

### rl_direct — math-500 52.4% (262/500) | math-hard 2.9% (30/1032) | pass@8 7.0% | RL-set acc 0.342->0.645 (peak 0.668@280, dip 0.339@390)
- **Asked:** The previous RL run had nothing to amplify: 30-55% of groups were all-right or all-wrong, so half the compute produced no gradient. With prompts restricted to the band this model actually solves 20-80% of the time, does accuracy move at all?
- **Learned:** Direct RL on the base is a near-null: math-500 +0.8 over base (52.4 vs 51.6, within noise), math-hard flat at 2.9%, and pass@8 DROPPED 3-4.6 points (7.0% vs the base's 10.1-11.6%) -- the one frontier where the base was strongest. The run nearly died mid-way: RL-set acc peaked 0.668 at step 280, collapsed to 0.339 by step 390 (loss x120, generation time 44s->152s as generations ran to the 320-token cap), then recovered to 0.645 by 500. The SFT+RL path never had this excursion -- SFT's compressed generations (40-char median vs the base's 104) leave no room to run away, so SFT regularizes the RL loop even though its own weights are worse. The base's wider pass@8 gap (+10.1% vs +5.4%) did not translate into more amplification: the band was 1048 problems, comparable to the SFT run's, but the recovery overfit to band easy wins and narrowed sampling diversity. Workflow forensics (same day): gen-time runaway LED the collapse (44s->158s, lockstep with the acc drop, loss x122 lagged 40-60 steps); gnorm and degen were passengers (degen fell during the crash). The recovery to 0.645 was a noisy overfit equilibrium, not restored capability -- pass@1 recovered while pass@8 fell, the signature of output-distribution mode narrowing that weight diffs cannot see. The base's broad output distribution is unstable under on-policy RL; SFT's length compression is an implicit regularizer. Separately: the band sits inside the capability envelope (easy parametrizations of L3/L4 programs), so RL only polishes pass@k->pass@1 -- the transfer ceiling for BOTH runs.
- **So:** Keep SFT+RL as production (math-hard 4.1% still best). Next in order: (1) best-ckpt saving DONE (40fa735); (2) length-only SFT -- compress base generations to ~40 chars median, no math content -- then RL with KL 0.05: isolates compression as the active ingredient; (3) KL sweep 0.05/0.1; (4) band curriculum toward hard parametrizations (reverse reasoning, big numbers, multi-constraint) to break the transfer ceiling. DAPO resampling deprioritized: degen was a passenger, not the cause.

### throughput_bisect — no regression; batch 32 without grad_ckpt = 90K tok/s/gpu MFU 38%, batch 72 with grad_ckpt = 37K MFU 15%
- **Asked:** The first 8-GPU run measured 37K tok/s/gpu at MFU 15% against runs/ablation_base.log's 91K at MFU 38%, which would turn a 4.4h pretrain into 10.8h. Was that a real regression introduced between aa3a8b9 and HEAD, and if so which change?
- **Learned:** There was no regression. Checking out aa3a8b9 -- the commit that produced the 91K figure -- and running it at its own committed Cfg (batch 72, grad_ckpt False) OOMs on 8xH20 exactly like HEAD, so 91K cannot have come from the configuration it was being compared against; the pod's working copy at the time must have carried different values. Three suspects were eliminated on the way, each worth knowing on its own: doc_mask is free (36K with, 37K without, and it is not what exhausts memory -- batch 72 without grad_ckpt OOMs with doc_mask off too); GPU count is not it (6 GPUs give 37K/gpu, same as 8); and grad_ckpt is not independently slow. What is true is that Cfg.batch=72 has never fit without gradient checkpointing at 206M/seq4096 on a 95GB card -- it needs >95GB against 52GB with checkpointing -- and that paying for checkpointing to keep a large batch costs 2.4x, while simply shrinking the batch costs nothing: 90K at batch 32, 89K at 24, 84K at 16.
- **So:** Pretrain at batch 32 (or the largest batch that still fits with AttnRes) with grad_ckpt OFF. Cfg.batch's default of 72 is a trap -- it silently forces the slow path -- and its comment claiming 97GB is enough is wrong. Separately: comparing a throughput number against a log whose configuration was never recorded cost about 90 minutes; runs/*.log gets no config header because it is written by runlog while the header goes through print.

### rl_k4 — rc=0 | step 500/500 acc 0.635 loss 0.0013 gnorm 10.856 gen 37s degen 13
- **Asked:** The previous RL run had nothing to amplify: 30-55% of groups were all-right or all-wrong, so half the compute produced no gradient. With prompts restricted to the band this model actually solves 20-80% of the time, does accuracy move at all?

### rl_k4 — rc=1 | step 90/500 acc 0.016 loss 0.0000 gnorm 0.350 gen 81s degen 23
- **Asked:** The previous RL run had nothing to amplify: 30-55% of groups were all-right or all-wrong, so half the compute produced no gradient. With prompts restricted to the band this model actually solves 20-80% of the time, does accuracy move at all?

### doc_mask_ab — no_doc_mask 9m04s / docmask 8m18s, both exit 0, both 32 recompiles
- **Asked:** Does --doc_mask (default ON since d5db37b) actually work on GPU, and what does the varlen FlashKDA path cost? It had never been run on a GPU, and the concern was a torch.compile recompile storm from the per-batch cu_seqlens length plus the FlashKDA 'A_log must be float32' error on the varlen path.
- **Learned:** doc_mask is free. mark_dynamic(cu, 0) does its job: the two arms recompile exactly the same number of times, so the varying cu_seqlens length triggers nothing. The 32 recompiles come from Block.forward specializing on mixer type (guard: ___check_type_id(self._modules['mixer']) type=DeltaRecurrence), which is 2 variants over 12 layers and is unrelated to doc_mask. No FlashKDA A_log error — the .float() patch is still in the pod's site-packages, which is itself the risk: it is not in the repo and dies with the container.
- **So:** Keep doc_mask ON for the pretrain. Neither arm's wall-clock is a throughput measurement — both were dominated by compile and by the epoch-end validation, which is what led to capping that validation at Cfg.val_batches_full.

### ab2_attnres_vs_base — both arms died at 2m43s: CUDA OOM, 94.9/95.2 GiB on every rank
- **Asked:** Does Attention Residuals (softmax over depth instead of the residual sum) actually lower loss at 200M/L=12, and what does it cost in throughput? The earlier base-only arm ran on stale code (no doc_mask, old dt_bias init, short_conv in the wrong LR group), so both arms are re-run on current code to make the comparison mean anything.
- **Learned:** The A/B never ran; what it found instead is that Cfg.batch=72 with Cfg.grad_ckpt=False does not fit on an H20 at all. Batch 48 OOMs identically (93.5 GiB), so the earlier 6-GPU ablation_base that reached 500 steps at batch 72 must have run the pod's older train.py, back when grad_ckpt still defaulted to True — the default was flipped to False without re-measuring what batch that leaves room for.
- **So:** Ablations move to --grad_ckpt. An adversarial review then measured that Full AttnRes at L=12 makes Inductor save one [B,T,D] tensor per (consumer, source) pair — 325 of them, 196 GB/GPU at batch 72 — so the sweep now compares base / attn_res_blocks=4 / attn_res_blocks=2 / Full rather than assuming Full is affordable.

### rlvr_gspo — stopped at step 90/500: acc 1.6-2.3%, gnorm 0.35-0.43, ~30% of groups degenerate
- **Learned:** A second copy of the run survived the first stop (duplicate launch on port 29541) and got to step 90 — 90 steps of flat accuracy with a stable gradient norm confirms the anti-collapse fixes hold, and equally confirms there is no signal to amplify: 1.6-2.3% sampled at T=0.9 against 31% greedy on the same distribution from the same checkpoint. Gold quality is not the cause (22-agent audit: 0.84% bad).
- **So:** Abandon RL on the old architecture. Pivot to a fresh pretrain on the new architecture (AttnRes + fused GEMMs + doc_mask + domain mix), because a 31%-greedy/2%-sampled base has no exploitable pass@k gap regardless of the RL objective.

### sft_v5_hard — math-hard: 32/1032 = 3.1% (L3 4%, L4 3%)
- **Learned:** The hard eval separates checkpoints that math-500 cannot: v4 scored 0.8% and v5 3.1% there, while on math-500 they are 32.0% vs 31.0% (indistinguishable). Absolute level is still near-zero — the model gets almost every arithmetic step wrong on L3/L4.
- **So:** math_hard_eval_1k is now the primary metric; math-500 is kept only as a regression tripwire.

### rlvr_stage2 — math-500 11.2% COLLAPSED, ckpt deleted
- **Asked:** Can GRPO with a verifiable boxed reward lift a 32% model?
- **Learned:** No — collapsed to 11.2%. Three causes, none of them the choice of RL algorithm: T=0.1 made the 8 samples per prompt near-identical so most groups had std=0 and zero advantage; there was no KL term or reference model, so 500 steps drifted unanchored; and at 32% pass@1 there was little gap between pass@1 and pass@8 for RL to amplify.
- **So:** No RL until SFT clears ~60% and pass@8 minus pass@1 is at least 15 points. When RL resumes: sequence-level ratio (GSPO), T around 0.8, KL beta 0.01-0.04, and only train on prompts with a 20-80% solve rate (the LFM-1.3B-Math selection rule).

### sft_v2_stage1 — math-500 26.4% (worse: lost openo1/school_math_r1 volume)
- **Asked:** Redo stage-1 from the pretrain checkpoint using only verified short-format data — better than the original mix?
- **Learned:** No — 26.4%. The rebuilt mix dropped openo1 (77K) and the full-length school_math_r1_zh (223K) in exchange for 110K filtered rows. Filtering raised per-row quality and lowered total information; accuracy followed the total, not the quality.
- **So:** Never let a quality filter shrink the corpus without replacing the volume. Verify aggressively, then backfill to at least the original token count.

### pretrain_k3_v2 — pretrain ckpt_k3-mla_2b_step2000.pt
- **Asked:** Does the K3 hybrid (KDA + Gated MLA, hd=128) train stably in native FP8 at 206M?
- **Learned:** Yes — 2.26B tokens, 3 epochs, no NaN. FP8 e4m3 both directions is stable as long as activation checkpointing is on (root cause of that dependency found later, see perf_sweep).
- **So:** K3 becomes the base for every downstream stage; ckpt_k3-mla_2b_step2000.pt is the only valid pretrain checkpoint (the old ckpt.pt is a different architecture and will not load).

### sft_stage1 — math-500 32.2% (baseline)
- **Asked:** What does the model score on Chinese grade-school math after a normal instruction SFT?
- **Learned:** 32.2% on the 500-problem holdout. The CoT is fluent and well-formed; the arithmetic inside it is wrong. Fluency and correctness are decoupled at this scale.
- **So:** Treat 32.2% as the number every later experiment must beat. Stop reading loss as progress — only holdout accuracy counts.

### sft_math_stage2 — math-500 32.2%
- **Asked:** Does a second SFT pass on math-only data lift math accuracy?
- **Learned:** No — 32.2%, unchanged. Stage-1 had already seen all of school_math, so stage-2 added no information, only more passes over the same rows.
- **So:** Stop doing stage-2 passes over data stage-1 already contains. New capability needs new data, not more epochs.

### sft_short_stage2 — math-500 31.6% (format alone no gain)
- **Asked:** Is the DeepSeek-style 'restate then rewrite' answer format what is holding accuracy back?
- **Learned:** No — 31.6%. The format did change (output halved in length, no second rewrite, boxed rate near 100%) but accuracy did not move. Format is a token-efficiency lever, not an accuracy lever.
- **So:** Keep the short format for cheaper generation and cleaner verification, but stop expecting accuracy from formatting work.

### k4_11b_lr05 — ep 1/1 train 2.005 val 2.086 18015s
- **Asked:** Same run as k4_11b_0827 with every optimizer lr halved. That run's loss bottomed at 3.447 by step 610 then climbed to 4.359 by 1060 with val 3.03->3.56, at a constant peak lr: too large a step for 786K tokens per update, 2.25x smaller than the batch these nanochat rates were tuned for. Does halving hold the descent past step 1000, or is the instability AttnRes rather than the lr?

### k4_11b_0827 — math-hard 2.9% (ckpt_k4_11b_lr05)
- **Asked:** First pretrain on the repaired pipeline and the new architecture, from scratch. Everything upstream changed at once, so the number that decides this run is post-SFT math-hard against the old base's 3.1%, not the pretrain loss against any earlier curve. What changed: fineweb-2 added (its source path had never resolved), 59% of it converted from Traditional to Simplified, the eval-holdout leak through the QA join closed (746 contaminated docs, 496 of the 500 math-500 questions), the domain mix sized to the corpus that exists and rebalanced so the anneal actually receives math, AttnRes enabled for the first time in a completed run, and batch dropped from 72 to 24 so gradient checkpointing is off (2.4x throughput).
- **Learned:** Base for the k4 line; indistinguishable from k5_clean_0827 on math (math-500 51.6 vs 51.2 p=0.899, math-hard 2.9 vs 1.9 p=0.152).
- **So:** Row closed retroactively 2026-08-29: the run finished and was scored, the row was never closed. Score is in the ledger under ckpt_k4_11b_lr05.

### rlvr_gspo2 — stopped at ~step 45/500: acc 1.4-2.2%, gnorm 0.31-0.42, 55% of groups degenerate
- **Asked:** The first RLVR run collapsed 32 to 11 percent. Do the three identified causes explain it: T=0.1 zero-variance groups, no KL anchor, and forced-EOS padding inside the loss mask? With T=0.9, a k3 KL anchor, sequence-level GSPO clipping, and generations truncated at their own EOS, does RL lift a 3.1 percent hard-eval model instead of destroying it?
- **Learned:** The anti-collapse fixes hold: 45 steps flat with a stable gradient norm, against the previous run that fell 32%->11% over 500. But there is nothing to amplify - sampled accuracy on the RL set is 1.4-2.2% at T=0.9 while the same checkpoint scores 31% greedy on the same distribution, and 55% of groups are all-wrong or all-correct so half the compute produces zero gradient. An independent 22-agent audit of 119 problems put the gold error rate at 0.84%, so this is genuine difficulty, not broken rewards.
- **So:** No RL until SFT clears roughly 60% on math-500 and pass@8 minus pass@1 is a real gap. GPUs go to the first SFT run on the repaired pipeline: holdout exclusion, corrected gold-answer extraction, greedy packing, fixed step verifier, dt_bias init, training-time softcap, short_conv LR group.

### rlvr_gspo — aborted: duplicate launch shared port 29540 and one log file
- **Asked:** The first RLVR run collapsed 32 to 11 percent. Do the three identified causes explain it: T=0.1 zero-variance groups, no KL anchor, and forced-EOS padding inside the loss mask? With T=0.9, a k3 KL anchor, sequence-level GSPO clipping, and generations truncated at their own EOS, does RL lift a 3.1 percent hard-eval model instead of destroying it?
- **Learned:** A pod-side nohup keeps running even when the local tool call is rejected right after, so the first launch was already live; the retry died on EADDRINUSE and both wrote the same log.
- **So:** Launch through this script: pgrep+port guard, unique port, dedicated log.

### rlvr_gspo — NOT RECORDED
- **Asked:** The first RLVR run collapsed 32%->11%. Do the three identified causes explain it: T=0.1 zero-variance groups, no KL anchor, and forced-EOS padding inside the loss mask? With T=0.9, a k3 KL anchor, sequence-level GSPO clipping, and generations truncated at their own EOS, does RL now lift a 3.1% hard-eval model instead of destroying it?
- **Learned:** Superseded: the pod log (merged 2026-08-29) carries two later rlvr_gspo rows -- one aborted on a duplicate launch sharing port 29600, one stopped at step 90/500 with acc 1.6-2.3%. This row is the launch, not a distinct measurement.
- **So:** Closed as killed 2026-08-29. Read the 20:36 row for the outcome. The RL question is reopened from scratch in the roadmap regardless: the 3.1->4.1 math-hard move has never been tested for significance against a +-1.1pt instrument.

### ablation_attnres — NOT RECORDED
- **Asked:** Does Attention Residuals (softmax over depth) lower pretrain loss at equal tokens, and what is the torchao FP8 throughput vs 393K tok/s on the legacy path?
- **Learned:** No result was ever written to this row. --attn_res --attn_res_blocks 4 is in the standing recipe, so a decision was taken, but the measurement behind it is not in this log and cannot be reconstructed from it.
- **So:** Closed as killed 2026-08-29, NOT as ok. AttnRes stays in the recipe on the strength of an unrecorded run; harness gaps should keep saying so.

### sft_v5 — math-500: 155/500 = 31.0%
- **Learned:** Adding 76.7K program-generated rows (756 programs) on top of v4 changed nothing: 31.0% vs v4 32.0%, within noise. Four runs now sit at 31-32%. Also confirmed the FP8 NaN fix in production: grad_ckpt off ran 1150 steps with zero NaN at 393K tok/s vs 271K with it (+45%).
- **So:** Stop iterating on this holdout ��� it is saturated. Judge future runs on math_hard_eval_1k (same ckpt scores 0.8% there). Next levers are architecture (AttnRes from aupai-05) and genuinely out-of-distribution data (TinyGSM), not more same-distribution rows.

### sft_v4 — TOTAL math-500: 160/500 = 32.0%
- **Asked:** With Ape210K capped at 10% and openo1/s1k restored, does 2.4x the loss tokens of v3 beat the 32.2% baseline?
- **Learned:** Recovered the regression (32.0%) but did not beat the baseline, despite 175M loss tokens against v3's 72M. Three runs now sit within 0.2 points of 32%: this is a plateau, not noise. Generation is clean (132 chars, 89.6% boxed) and still arithmetically wrong. The corpus is also less diverse than its row count suggests — deduplication dropped 126K of Mxode's 220K rows as Belle prompts rewritten.
- **So:** Stop adding data drawn from the same distribution. Next: (a) the L3/L4 hard eval set, to establish whether 32% is a model ceiling or holdout saturation, since all 500 holdout problems come from Belle; (b) genuinely heterogeneous data — TinyGSM's 11.8M rows are MIT-licensed and share no distribution with Belle; (c) drop grad_ckpt now that the NaN cause is fixed, for ~25% faster iteration.

### sft_v3 — TOTAL math-500: 134/500 = 26.8%
- **Asked:** Does 489K verified rows from open Chinese datasets (Ape210K, Belle, Mxode, math23k) beat the baseline?
- **Learned:** No — 26.8%, a regression. Ape210K was 32% of the mix and its rows are a single line ('列式：5/3 = 1.667') with no intermediate steps. Average generation fell to 136 characters and the model started answering division with quotient-and-remainder where the gold answer is a fraction. A large share of one-line data actively destroys multi-step reasoning.
- **So:** Cap any single-line source at roughly 10% of the mix. Judge a math corpus by the step count of its solutions, not by its row count.

### perf_sweep — batch 32=29s/10步, 64=57s/10步 -> 吞吐恒定271K tok/s (compute-bound); no_grad_ckpt NaN -> 根因见 850369a
- **Asked:** Where is SFT throughput going, and why does disabling activation checkpointing produce NaN?
- **Learned:** Throughput is compute-bound at ~271K tok/s across 6 H20s — doubling the batch doubles step time exactly, so batch tuning is a dead end. The NaN has nothing to do with FP8 range (input amax 936 against an e4m3 limit of 448, scaling correct): Inductor's min-cut partitioner recomputes FP8LinearFunction's saved fp8 tensors in the backward graph, re-dividing already-scaled values. Isolated by bisection — eager is clean at batch 2 and 8 with Muon and 130 steps; adding torch.compile NaNs at step 1 with a finite forward and all 152 parameter gradients NaN.
- **So:** @torch._dynamo.disable on FP8Linear.forward (commit 850369a); verified 60 clean steps with compile on and grad_ckpt off. Activation checkpointing can now be dropped for roughly 25% more speed. Stop tuning batch size for throughput.

<details><summary>Commands</summary>

- **k8_proc_replay** (`2026-08-29 16:51`): `torchrun --nproc_per_node=4 sft_math.py --resume ckpt_k8_v3_fone.pt --sft_path data/sft/proc_v1_replay.pt --out ckpt_k8_proc_replay.pt --batch 16 --epochs 3 --lr_scale 0.1`
- **k8_v3_fone** (`2026-08-29 16:41`): `torchrun --nproc_per_node=7 train.py --name k8_v3_fone --fp8 --fone --attn_res --attn_res_blocks 4 --mix data/mix_v3.json --warmup 150 --lr_scale 0.5`
- **k8_proc_sft** (`2026-08-29 16:41`): `torchrun --nproc_per_node=4 sft_math.py --resume ckpt_k8_v3_fone.pt --sft_path data/sft/proc_v1.pt --out ckpt_k8_proc.pt --batch 16 --epochs 3 --lr_scale 0.1`
- **k8_v3_fone** (`2026-08-29 06:15`): `torchrun --nproc_per_node=7 --master_port=29600 train.py --name k8_v3_fone --mix data/mix_v3.json --fone --fp8 --attn_res --attn_res_blocks 4 --warmup 150 --lr_scale 0.5`
- **sft_k5_v11** (`2026-08-28 18:00`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k5_clean_0827.pt --sft_path data/sft/sft_v11_k5vocab.pt --out ckpt_sft_k5_v11.pt --batch 24 --epochs 6 --lr_scale 0.1`
- **sft_k5_v9_ep2** (`2026-08-28 17:35`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k5_clean_0827.pt --sft_path data/sft/sft_v9_k5vocab.pt --out ckpt_sft_k5_v9_ep2.pt --batch 24 --epochs 2 --lr_scale 0.1`
- **fone_arith** (`2026-08-28 17:24`): `eqcheck over saved math-hard generations`
- **sft_k5_v10** (`2026-08-28 17:14`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k5_clean_0827.pt --sft_path data/sft/sft_v10_k5vocab.pt --out ckpt_sft_k5_v10.pt --batch 24 --epochs 6 --lr_scale 0.1`
- **sft_k5_v9** (`2026-08-28 16:00`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k5_clean_0827.pt --sft_path data/sft/sft_v9_k5vocab.pt --out ckpt_sft_k5_v9.pt --batch 24 --epochs 6 --lr_scale 0.1`
- **sft_k6_mix6** (`2026-08-28 15:40`): ``
- **sft_k5_ctrl** (`2026-08-28 15:40`): ``
- **sft_k5_ctrl** (`2026-08-28 15:06`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k5_clean_0827.pt --sft_path data/sft/sft_mix_k5vocab.pt --out ckpt_sft_k5_ctrl.pt --batch 24 --epochs 6 --lr_scale 0.1`
- **sft_k5_mix6** (`2026-08-28 14:59`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k5_clean_0827.pt --sft_path data/sft/sft_mix_plain.pt --out ckpt_sft_k5_mix6.pt --batch 24 --epochs 6 --lr_scale 0.1`
- **sft_k6_mix6** (`2026-08-28 14:23`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k6_fone.pt --sft_path data/sft/sft_mix_fone.pt --out ckpt_sft_k6_mix6.pt --batch 24 --epochs 6 --lr_scale 0.1`
- **k6_base_eval** (`2026-08-28 13:58`): `scripts/eval_hard.sh ckpt_k6_fone.pt 3; eval/run_eval.py --benchmarks mmlu`
- **sft_k6_mix** (`2026-08-28 13:58`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k6_fone.pt --sft_path data/sft/sft_mix_fone.pt --out ckpt_sft_k6_mix.pt --batch 24 --epochs 2 --lr_scale 0.1`
- **sft_k5** (`2026-08-28 10:14`): `torchrun --nproc_per_node=8 sft_math.py --resume ckpt_k5_clean_0827.pt --sft_path data/sft/sft_k4.pt --out ckpt_sft_k5.pt`
- **k5_clean_0827** (`2026-08-28 09:03`): ``
- **k6_fone** (`2026-08-28 07:03`): `torchrun --nproc_per_node=7 train.py --fp8 --fone --attn_res --attn_res_blocks 4 --warmup 150 --lr_scale 0.5 --name k6_fone --track`
- **k5_clean_0827** (`2026-08-28 02:58`): `torchrun --nproc_per_node=7 train.py --fp8 --attn_res --attn_res_blocks 4 --warmup 150 --lr_scale 0.5 --name k5_clean_0827`
- **sft_k5** (`2026-08-28 01:00`): `torchrun --nproc_per_node=8 sft_math.py --resume ckpt_k5_clean_0827.pt --sft_path data/sft/sft_k4.pt --out ckpt_sft_k5.pt `
- **k5_clean_0827** (`2026-08-27 13:16`): `torchrun --nproc_per_node=7 train.py --fp8 --name k5_clean_0827 --attn_res --attn_res_blocks 4 --warmup 150 --lr_scale 0.5`
- **rl_k4** (`2026-08-27 10:46`): ``
- **k4_11b_lr05** (`2026-08-27 10:46`): ``
- **sft_k4** (`2026-08-27 06:14`): ``
- **rl_direct** (`2026-08-27 03:45`): `torchrun --nproc_per_node=8 algorithms/rlvr.py --resume ckpt_k4_11b_lr05.pt --data data/rl/rl_band.jsonl --steps 500`
- **throughput_bisect** (`2026-08-27 00:21`): `torchrun --nproc_per_node=8 train.py --fp8 --mix '' --max_steps 40 [--batch N] [--grad_ckpt] [--no_doc_mask] [--attn_res]`
- **rl_k4** (`2026-08-26 23:17`): `torchrun --nproc_per_node=8 algorithms/rlvr.py --resume ckpt_sft_k4.pt --data data/rl/rl_band.jsonl --steps 500`
- **rl_k4** (`2026-08-26 23:16`): `torchrun --nproc_per_node=8 algorithms/rlvr.py --resume ckpt_sft_k4.pt --data data/rl/rl_band.jsonl --steps 500`
- **doc_mask_ab** (`2026-08-26 22:04`): `torchrun --nproc_per_node=2 train.py --fp8 --mix '' --max_steps 6 --batch 8 --name dm_{nodocmask,docmask} [--no_doc_mask]`
- **sft_k4** (`2026-08-26 22:03`): `torchrun --nproc_per_node=8 sft_math.py --resume ckpt_k4_11b_lr05.pt --sft_path data/sft/sft_k4.pt --out ckpt_sft_k4.pt --batch 24 --epochs 2 --lr_scale 0.1`
- **sft_k4** (`2026-08-26 21:56`): `torchrun --nproc_per_node=8 sft_math.py --resume ckpt_k4_11b_lr05.pt --sft_path data/sft/sft_k4.pt --out ckpt_sft_k4.pt --batch 24 --epochs 2 --lr_scale 0.1`
- **ab2_attnres_vs_base** (`2026-08-26 20:55`): `torchrun --nproc_per_node=8 train.py --fp8 --mix '' --max_steps 500 --name ab2_{base,attnres} [--attn_res]`
- **rlvr_gspo** (`2026-08-26 20:36`): ``
- **sft_v5_hard** (`2026-08-26 19:34`): ``
- **rlvr_stage2** (`2026-08-26 17:39`): `torchrun --nproc_per_node=6 algorithms/rlvr.py --resume ckpt_sft_math.pt --steps 500 -T 0.1`
- **sft_v2_stage1** (`2026-08-26 17:39`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k3-mla_2b_step2000.pt --sft_path sft_short_all.pt --batch 32 --epochs 2`
- **pretrain_k3_v2** (`2026-08-26 17:38`): `torchrun --nproc_per_node=6 train.py`
- **sft_stage1** (`2026-08-26 17:38`): `torchrun --nproc_per_node=6 sft.py`
- **sft_math_stage2** (`2026-08-26 17:38`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_sft.pt --sft_path sft_math.pt`
- **sft_short_stage2** (`2026-08-26 17:38`): `torchrun --nproc_per_node=4 sft_math.py --resume ckpt_sft.pt --sft_path sft_math_short.pt`
- **k4_11b_lr05** (`2026-08-26 16:53`): `torchrun --nproc_per_node=8 --master_port=29652 train.py --name k4_11b_lr05 --fp8 --attn_res --attn_res_blocks 4 --batch 24 --warmup 150 --lr_scale 0.5`
- **k4_11b_0827** (`2026-08-26 16:26`): `torchrun --nproc_per_node=8 --master_port=29650 train.py --name k4_11b_0827 --fp8 --attn_res --attn_res_blocks 4 --batch 24 --warmup 150`
- **rlvr_gspo2** (`2026-08-26 12:07`): `torchrun --nproc_per_node=6 --master_port=29541 -m algorithms.rlvr --resume ckpt_sft_v5.pt --data data/rl/rlvr_clean.jsonl --steps 500 --temperature 0.9 --top_p 0.95 --max_new 320 --kl_beta 0.02`
- **rlvr_gspo** (`2026-08-26 12:04`): `torchrun --nproc_per_node=6 -m algorithms.rlvr --resume ckpt_sft_v5.pt --data data/rl/rlvr_clean.jsonl --steps 500 --temperature 0.9 --top_p 0.95 --max_new 320 --kl_beta 0.02`
- **rlvr_gspo** (`2026-08-26 12:01`): `torchrun --nproc_per_node=6 algorithms/rlvr.py --resume ckpt_sft_v5.pt --data data/rl/rlvr_clean.jsonl --steps 500 --temperature 0.9 --top_p 0.95 --max_new 320 --kl_beta 0.02`
- **ablation_attnres** (`2026-08-26 11:39`): `NGPU=6 STEPS=500 scripts/run_ablation.sh`
- **sft_v5** (`2026-08-26 11:30`): ``
- **sft_v5** (`2026-08-26 10:58`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k3-mla_2b_step2000.pt --sft_path data/sft/sft_v5.pt --out ckpt_sft_v5.pt --batch 16 --epochs 2 --lr_scale 0.1 --no_grad_ckpt`
- **sft_v5** (`2026-08-26 10:54`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k3-mla_2b_step2000.pt --sft_path data/sft/sft_v5.pt --out ckpt_sft_v5.pt --batch 32 --epochs 2 --lr_scale 0.1 --no_grad_ckpt`
- **sft_v4** (`2026-08-26 10:13`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k3-mla_2b_step2000.pt --sft_path data/sft/sft_v4.pt --out ckpt_sft_v4.pt --batch 32 --epochs 2 --lr_scale 0.1`
- **sft_v3** (`2026-08-26 09:49`): `torchrun --nproc_per_node=6 sft_math.py --resume ckpt_k3-mla_2b_step2000.pt --sft_path data/sft/sft_v3.pt --out ckpt_sft_v3.pt --batch 32 --epochs 3 --lr_scale 0.1`
- **perf_sweep** (`2026-08-26 09:39`): `sft_math.py --max_steps 30 batch sweep 24/32/64/96`

</details>
