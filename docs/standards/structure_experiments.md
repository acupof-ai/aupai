---
question: 200M 阶梯下的结构实验：AttnRes 开关、按块类分 lr（比例延后）—— 四个 run 花多少、读什么、两种结果各触发什么
status: recorded
source: b0-12（`27ab188`，fb 裁定 2026-09-02，pair tilerl）。**前身 b0-9、b0-11 都已 dropped。**
  b0-9 的第三问建在假前提上：把 `0.61` 当成 `1/sqrt(L)` 的深度 lr 规则，而 `0.61` 是
  experiments 第 171 行的 `--lr_scale`（ledger 旋钮被写成规则），`1/sqrt(32) = 0.177`。
  **b0-11 换成"按块类分 lr"，输入是实测的 3.4–3.7× 增长比**；b0-11 本身则因本页
  §1 算出的三件事被替换：**100M 这一级比 200M@1B 贵、7:1 在 L12/L18 不是整数、
  四个 run 而非三个**。
  基线事实全部从 `ckpt_p500m_20b_0902.pt.step2500` 读出，不是从代码或论文推。
  生产阶梯 200M@4B → 300M@6B 由用户 2026-09-02 10:06Z 定
  （`docs/lessons/stop_window_2026-09-02.md` 末节）。
---

# 四个 run，200M@1B：先填 AttnRes 的空白，再试按块类分 lr

**这页的规矩：一个 run 一个变量，读数在开跑前写死，两种结果各对应一个动作。**
没有"看情况再说"的分支——那等于没有预注册。

## 0. 基线事实（读出来的，不是推的）

从 `ckpt_p500m_20b_0902.pt.step2500` 读：

| 事实 | 值 | 怎么读到 |
|---|---|---|
| 块布局 | **32 块 = 24 KDA + 8 MLA，MLA 在 3/7/11/…/31** | `blocks.<i>.mixer.A_log` 在不在 |
| KDA 参数占比 | mixer 126.2M + ffn 226.5M = **352.7M（块参数的 67%）** | 2D 权重逐个求和 |
| 每块参数 | KDA 14.71M / MLA 13.38M（**近乎相等**） | 同上 |
| KDA/MLA 范数增长比 | **L32: 3.74→3.43→7.96×；L12: 1.63→2.65×**（五个区间，**两个规模都不稳定，见 §3**；`facts/efficiency.json#eff.kda_mla_growth_ratio_l32` / `#eff.kda_mla_growth_ratio_l12`） | `scripts/ckpt_health.py` 按块类拆 |
| 张量范数中位 | **KDA 42.88 > MLA 33.77** | 同上 |
| FP8 覆盖 | 块内 linear **全覆盖**；只有 LM head 不走（6.38%，已判决 no-ship） | `_fp8_ok` 逐个核 |

**那个 3.4–3.7× 不是小分母造成的**：如果 KDA 权重更小，同样的绝对更新会显示成更大的相对增长。
**实测 KDA 范数更大（42.88 vs 33.77），每块参数量也几乎相等**，所以两个最可能的伪影方向都排除了。
KDA 块**又大又长得更快**，这是 R3-lr 的动机（§3）。

## 1. 规模与预算：四个 run，200M@1B，排在 p300m 之后

**b0-12 的形状，三条都按本页算出的数落定（fb 裁定 2026-09-02）：**

**一、实验层 = 200M@1B，100M 这一级砍掉。**
吞吐用 **72K tok/s/gpu，30% MFU**——`facts/efficiency.json#eff.microbatch_32_oom`
里 t52 pair1（2026-08-31）的**基线臂 batch 16 accum 2**，seq 4096、fp8、206M/L12/d1024，
**p200m_4b 启动行的同一个配置**（`stop_window` 写的是 `--batch 16 --accum 2`）。

**本页前一版用的是 `eff.fb_mfu` 的 73K「batch 32」，那个 batch 是错的**（fb 更正
2026-09-02）：那条 fact 的 source 是 "aupai-fb briefing"，**batch 32 从来没被量过，
而且第二天就被实测推翻**——t52 量到 b32a1 在 93.8/95.2 GB OOM，判决是「保持 micro-batch 16」。
**我把一句 briefing 读成了一个被测过的配置**，然后拿它去给别人的 OOM 归因（见 §6 末）。

| 方案 | 卡时/run | 吞吐来源 |
|---|---|---|
| **200M @1B（采用）** | **3.86** | **实测**（`eff.microbatch_32_oom` 基线臂，同配置） |
| 100M @2B，72K | 7.7 | 外推 |
| 100M @2B，144K（盈亏平衡） | 3.86 | 外推 |

**100M 要便宜过 200M@1B，得跑到 144K tok/s/gpu = 实测值的 2.00 倍**，没有依据。

**这个 2.00 倍不依赖吞吐的具体值，这是本节唯一稳的地方**：200M 跑 1B、100M 跑 2B，
**token 数差 2 倍，所以盈亏平衡永远是「2 倍吞吐」，不论 T 是 72K 还是 73K 还是别的**。
73K → 72K 只把绝对卡时挪了 1.4%（3.81 → 3.86），**三条结论一条都没动**。
而 200M 本来就是验收所在那一级，**省掉"能否外推到 200M"这一跳**。

**启动成本不会翻转这个结论，它在等式两边都出现。** 令启动秒数为 S：

```
200M@1B 墙钟 = 1e9/(8×72K) + S = 1,736 + S
100M@2B 墙钟 = 2e9/(8×T)   + S
100M 更便宜  ⟺  2e9/(8T) < 1,736  ⟺  T > 144K = 2×72K
```

**S 精确抵消。** 两个方案跑的是同一份代码、同一条缓存读路径，
**启动成本近似相等，所以它改变两边的绝对值而不改变谁更便宜**。
**唯一的翻转杠杆是 100M 自己的吞吐要到 144K**——实测值的两倍，而「两倍」这个
倍数由 token 比 2:1 决定，换任何吞吐值都不变。
（`--warmup` 也不是杠杆：它只改 lr 表，每步 token 数不变，见下。）
**"S 大到某个值就翻转"这个形状不成立**，因为它默认 S 只加在一边。

**二、硬上限四个 run。**

| run | 问题 | 配置（L12 d1024、1B token） |
|---|---|---|
| **B0** | 三问共用基线 | 3 MLA + 9 KDA、AttnRes ON、单一 base lr |
| **B0-seed2** | 定 sigma（**不可砍**） | 同 B0 换 seed |
| **R2-off** | （2）AttnRes | AttnRes OFF |
| **R3-lr** | （3a）分 lr | KDA 块 lr × 0.28 |

**4 × 3.86 = 15.4 卡时 = 八卡 1.93 小时。** 参照：
**这正好等于 p200m_4b 生产 run 本身的成本（4B token 同一吞吐 = 15.4 卡时），
是 p200m + p300m 合计（38.6 卡时）的 40%。**
**排在 p300m 之后串行跑**，不与生产 run 抢卡。

### 四个臂的旋钮：照 p200m 启动行，只改 `--warmup`

**其余旋钮全部照 `stop_window_2026-09-02.md` 的 p200m 启动行**
（`--dim 1024 --layers 12 --heads 8 --ffn_hidden 3072 --batch 16 --accum 2
--no-grad_ckpt --lr_scale 1.0 --warmdown 0.1 --anneal_frac 0 --save_every 500`），
**batch 16 accum 2 而不是 32×1**：`eff.microbatch_32_oom` 实测 b32a1 在 93.8/95.2 GB
OOM，每步 token 数两者相同（16×2 = 32×1），**所以这不是一个可选的旋钮，是唯一装得下的写法**。
mix 是 500M 组成缩到 1B。**`--warmup` 是唯一要改的**（fb 定）。

**warmup 不影响成本，只影响 lr 表**——它改 lr 乘数（`train.py:1985`），
**每步 token 数不变**，所以它进这一节是可读性问题不是预算问题。
**（本页上一版把它当成摊薄吞吐的原因之一，那是错的，fb 更正。）**

**`--warmup 100` 有一个副作用要写明**：`train.py:1160` 的 Muon 动量 ramp 是
`0.85 + 0.10 × min(1, step/150)`，**一个在 L=12 上拟合、之后没再动过的常数**。
warmup 结束时动量走到哪里，随 warmup 值大幅移动：

| `--warmup` | 占 954 步 | warmup 结束时的动量 | 动量 ramp 进度 |
|---|---|---|---|
| 20（`Cfg` 默认） | 2.1% | 0.8633 | 13% |
| **100（本次采用）** | **10.5%** | **0.9167** | **67%** |
| 300（p200m 生产值） | 31.4% | 0.9500 | 100% |

**100 把两个常数的相对关系换了一个位置**：默认 20 下 lr 满值时动量才走 13%
（`docs/lessons/warmup_momentum_interaction.md` 记的就是这个），
**100 下变成 67%**。**这不构成反对 100 的理由**——它比 300 更接近生产 run 的
warmup 占比（生产 300/3815 = 7.9%，本次 100/954 = 10.5%，**按占比匹配应取 75**），
而且 `eff.warmup_absolute_not_fractional` 的证据是**单侧的**（2/3/20 三个点单调，
**20 以上从未测过**），所以 100 落在一个没有读数的区间里。

**生产 run 的续训实际用的是 `--warmup 300`，不是 100**（`resume2` 启动行，2026-09-02 14:32:43Z）。
**四个臂用 100，生产用 300** —— 这不是矛盾，是本节选 100 的结果；写在这里是为了
**任何拿臂的曲线对生产曲线的人先看到这一行**。

**含义：四个臂之间可比（同一个 warmup），但它们与 p200m 生产 run 的早期曲线不可比。**
**别把臂的前 100 步 loss 和生产 run 的前 300 步对着看**——lr 表和动量状态都不同。

**（1）比例延后，理由是空白硬度而不是重要性**：（2）的 OFF 臂在任何深度都没有配对读数
（§3），（3a）有 3.4–3.7× 的实测动机（§0），**（1）两者都没有**——现状 3:1 已经有
20B token 的运行记录，而"更稀疏会不会更好"目前只是猜想，没有任何读数指向它。
**5:1 作为 R1 的 B 臂保留在 §2 待用**，等（2）（3a）的结论回来再排。

**B0-seed2 不可砍**：0.02 nat 的门是 500M/0.2B 的 sigma-hat 0.0516 换算的，
**200M/1B 的 seed 方差没测过**。没有它，R2-off 和 R3-lr 的胜负都读不出来，
**那三个 run 一起白跑**。**两个 seed 是这个计划的最小可读单位，不是余量。**

**三、`7:1` 在这个阶梯上不存在。** MLA 数必须是整数：

| L=12（p200m） | 可达比例 |
|---|---|
| 2 MLA + 10 KDA | **5:1** |
| **3 MLA + 9 KDA** | **3:1（现状）** |
| 4 MLA + 8 KDA | 2:1 |
| 6 MLA + 6 KDA | 1:1 |

**7:1 在 L=12 要 1.5 个 MLA，在 L=18 要 2.25 个。** 本页上一版的 7:1 是从 500M 的
L=32 抄来的（那里 4 MLA 刚好 7:1）——**把一个只在 L=32 成立的配置写成了整个阶梯的臂**。

### "现状 3:1" 只对 L12 和 L32 说话（fb 指定本节由 b0 写明）

| run | L | 布局 | 比例 |
|---|---|---|---|
| p200m_4b | 12 | 3 MLA + 9 KDA | **3:1** |
| p300m_6b | 18 | **4 MLA + 14 KDA** | **3.5:1** |
| p500m（已停） | 32 | 8 MLA + 24 KDA | **3:1** |

**p300m 保持 L18**（用户定的参数级，`stop_window_2026-09-02.md`），
**所以它的比例是 3.5:1，不是 3:1**。**本页所有"现状 3:1"只指 L12 和 L32。**
含义有两条，都要写进任何采用比例结论的 recipe：

1. **结构实验在 L12 上跑，结论按比例往 L18 搬时不能照抄 MLA 个数。**
   若（1）将来定出 5:1，L=18 上 5:1 要 3 个 MLA（15 KDA），**不是把 4 改成 3 就完事**——
   MLA 的**位置**也要按新比例重排。
2. **p300m 今天的 3.5:1 不是任何实验的结论，是 L18 下 4 个 MLA 的算术结果。**
   把它当成"现状比例"会把一个取整副产品读成一个决定。

**上一版的成本报错 8 倍，机制值得记**：写的是
`2e9/(30e3×8) ≈ 8,333 卡秒 ≈ 2.3 卡时/run`。**8,333 是墙钟秒，不是卡秒**——
除以 8 卡之后再叫卡时，**等于把"卡时"里的"卡"约掉了**。
**卡时 = 墙钟 × 卡数**，同一个 30K 下真值 18.5 卡时。
**错不在 30K 那个外推（只差 2.4×），在单位。** 两个错叠起来报出 2.3 对着真实 7.6，
低报 3.3 倍——**而正是这个低报让"六个 100M run"看起来可接受，掩盖了第一条结论。**
（已收进 shape 表。）

## 2. R2-off：AttnRes 开关 —— **四个 run 里空白最硬的一个**

**问的数**：val loss 与 tok/s/gpu 的配对。
**A 臂 = AttnRes ON（现状 Full）** = B0，**B 臂 = OFF** = R2-off。

**这是真实空白，不是重复测量**：`facts/efficiency.json` 里所有 AttnRes 读数
（`eff.grad_ckpt_inverts_with_depth`：L=32 + AttnRes Full ON 下 grad_ckpt 代价 1.116×、
省 4.1× 峰值内存；`eff.depth_shape_matched_pair`）**全都是 ON 那一臂的**。
**OFF 臂在任何深度上都没有配对读数**，所以现在无法回答"AttnRes 值不值"。
**这个空白是三问里唯一"一个 run 就填满"的**——这是它进入四个 run 的理由，
也是（1）被延后的对照：（1）问的方向今天没有任何读数指向它。

**预注册读数**：0.02 nat 门（见 §4 的 sigma 说明）+ 吞吐差。**AttnRes OFF 必然更快**（少一路深度注意力），
所以读的是**它换来多少 loss**，不是"哪个快"。

### ON 臂的 source-read 计数：**325 / 2145 是对的，我上一版的"更正"作废**

**这一节整段是一次自我更正的记录，留着是因为错法比数值有用。**

`eff.grad_ckpt_inverts_with_depth` 记的 "325 at L=12 到 2145 at L=32、峰值 65 对 25"
**没有错**。我在 `d4a0f78` 里把它"改"成 181 / 1121（峰值 33 对 13），**理由是
`probes/t71_depth_lr_rule.py:130` 的 `source_reads(L, blocks=0)` 用了默认参数**，
并断言"真实调用是 `blocks=L`"。**那个断言是我编的，我没有去读默认值是什么。**

**实际的代码**：

```
train.py:219    attn_res_blocks = 0
model.py:330    n_blocks = min(n_sub, getattr(cfg, "attn_res_blocks", 0) or n_sub)  # 0 -> Full
```

`0 or n_sub` → `n_blocks = n_sub = 2L`，于是 `ar_block_ends = {1,2,…,2L}`，
**每个 sublayer 都是块边界、当场把自己的输出升进 `done`**——
**这正是 `blocks=0` 那一列，也正是生产 run 跑的东西**（fact 里
`"attn_res": "Full (attn_res_blocks=0, 25 sources)"` 出现五次，说的就是这个）。
**`blocks=L` 是一个没有任何 run 用过的配置**，我拿它当"真实"，
所以 181 / 1121 是一个不存在的世界的读数。

| | L=12 | L=32 | 比值 |
|---|---|---|---|
| **`blocks=0` = Full = 生产（对的）** | **325 reads / 25 peak** | **2145 / 65** | **6.60×** |
| `blocks=L`（我误当成真实的） | 181 / 13 | 1121 / 33 | 6.19× |

**错法的形状，和我以为自己在抓的那个正好相反**：我以为看见的是"把默认值当实测配置"
（`eff.fb_mfu` 的 batch 32，`gate_failure_shapes.md` §62）。**真实的错是
"看见 `=0` 就断定它不是实际配置"**——`0` 在这里是**哨兵值**，
`0 or n_sub` 把它翻译成"全开"。**我从一个参数名和一个字面量推出了语义，
没有读那一行的 `or`。** 与 §62 同源但方向相反：§62 是把话当成测量，
这一条是把**代码里读得到的事实**当成**可疑的默认值**——
**两次都是没去读那一行**。

**代价**：一条错的更正进了 commit（`d4a0f78`），并经 aupai-98 转给了 e1
要求改 `facts/`。**已发撤回**。`facts/` 我本来就不改，这次也没改——
**唯一实际损害是别人的时间**。

**两种结果各触发什么**：
- **OFF 的 loss 劣化 < 阈值** → **关掉**，把省下的吞吐写进 200M/300M 配置，
  并撤掉 `Block.sublayers()` 的必要性（**但 b0-8 的 `AttnRes` 构造期守卫保留**——
  它防的是"配置说开了实际没开"，与开关本身正交）。
- **OFF 的 loss 劣化 ≥ 阈值** → 保持 ON，**把这个配对读数落成 fact**，
  它是 AttnRes 第一次有 OFF 臂对照。

## 3. ~~R3-lr：按块类分 lr~~ —— **已撤（fb 裁定 2026-09-02，见 §6）**

**撤的理由不是"1.63× 太小"，是"1.63× 也是单对读数、且落在 warmup 后第一个窗口"**——
用它反推 0.61 会重复 0.28 的同一个错误：**拿一个未稳定的比值当一个常数**。
**b0-12 保留三个 run（B0、B0-seed2、R2-off），第四个 slot 空着**。

### 第四个 slot：**open，且已确定不是 lr**（1e 裁定 2026-09-02，第二区间读数触发）

上一版写的是"等第二个区间给出**稳定的** KDA/MLA 比，再决定第四问是 lr 还是别的"。
**第二个区间读完了，比值不稳定，所以那个条件是按预注册的否定分支落地的**：

| 规模/深度 | 区间 | 比值 |
|---|---|---|
| 500M / L32 | 1500→2000 | 3.74× |
| 500M / L32 | 2000→2500 | 3.43× |
| 500M / L32 | 2500→3000 | **7.96×** |
| 200M / L12 | 500→832（跨中断，仅 168 步续训） | 1.63× |
| 200M / L12 | **1000→1500（干净 500 步，全在 resume2 内）** | **2.65×** |

**五个区间、两个规模、两个深度，没有一个给出稳定比值**；L12 自己一个区间就涨了 63%
（1.63 → 2.65）。**所以第四个 slot 不填 lr**，理由是**这个量没有固定靶**——
不是"差距太小"（它现在是 2.65× 而不是 1.63×），而是**任何从单区间反推的系数只对那个区间成立**。
0.28 死于此（从 3.589 反推），0.61 同样死于此（从 1.63 反推），
**2.65 反推出的 0.377 会以同一方式死掉**，所以不再反推第三个系数。

**slot 保持 open，候选待第三区间（1500→2000）之后定。** 现在不填候选，
理由与上面同一条：在一个还在漂的量上挑第四问，就是让第四个 run 继承这次的错。
**填这个 slot 的前提是一个不依赖"比值稳定"的问题**——§3 第 2 条那种（只读 val loss、有固定靶）
是可用形状，但它属于 R3 而 R3 已撤，所以要新拟。

**§3 原有的 val-loss 读法保留给 R2**（fb 明确：不给 R3）。下面整节留作历史与推导记录。

### 原 R3-lr 的形状与两次系数作废（历史）

**fb 裁定的形状**：A 臂 = 现状（**无残差输出缩放、Muon 单一 base lr**），
B 臂**只改一件事**：KDA 块的 lr 乘一个 <1 的系数。
**残差缩放 (3b) 不做**——两臂同时改无法归因。

**b0-9 的第三问是什么、为什么作废**：它问"`1/sqrt(L) = 0.61` 在 Muon 下对不对"。
**那个 0.61 不是 `1/sqrt(L)`**——它是 experiments 第 171 行的 `--lr_scale 0.61`，
一个 ledger 旋钮；`1/sqrt(32) = 0.177`。**一个把旋钮值当成理论规则的问题，
两种结果都读不出东西**，所以整问撤掉。**3a 的输入换成实测量**（3.4–3.7× 增长比），
这是它和 b0-9 第三问的根本区别。

### 0.28 这个系数已经作废，原因是第三个区间（step3000 读数，2026-09-02 11:0xZ）

**先给三个区间的全貌**，把"范数增长超出 1.0 的部分"当作与 lr 一阶成正比，
等化系数 = MLA 超出量 / KDA 超出量：

| 区间 | KDA−1 | MLA−1 | 比 | 反推系数 |
|---|---|---|---|---|
| 1500→2000 | 0.088239 | 0.023570 | 3.74× | 0.267 |
| 2000→2500 | 0.040859 | 0.011895 | 3.43× | 0.291 |
| **2500→3000** | **0.049344** | **0.006199** | **7.96×** | **0.126** |

**0.28 是前两个区间的均值（3.589 → 0.279）。第三个区间是 7.96×，系数 0.126——
差一倍多。** 这不是噪声，两条分量各自都是单调的：

- **MLA 增长每个区间对折**：0.0236 → 0.0119 → 0.0062。
- **KDA 增长掉头往上**：0.0882 → 0.0409 → **0.0493**。

**所以 7.96× 主要是 MLA 减速更快造成的，不是 KDA 加速跑掉。**
**含义是致命的：这个比值不是一个稳定属性，它随训练进程漂移，所以"把它等化到 1"
没有一个固定的靶。** 用任何单一区间反推的系数，都只对那个区间成立。

**R3-lr 因此必须改，改法有两条，都要 fb 裁：**

1. **把靶从"比值"换成"比值的斜率"**：读数改为"分 lr 后，比值随 step 的漂移是否变平"，
   而不是"比值是否落在 [0.8, 1.25]"。**代价是这需要至少三个 checkpoint，1B/954 步
   在 `--save_every 500` 下只有两个**（step500、954 收尾），**所以要改成
   `--save_every 250`**——不加卡时，只多写几个 2.1GB 文件。
2. **或者放弃等化，改问一个有固定靶的问题**：B 臂 KDA lr × 0.28，
   **读数只留 val loss**（不劣于 A 臂即算"KDA 块可以用更低 lr 而不损失质量"）。
   **这一条今天就可读，且不依赖任何线性假设。**

**我的判断：选 2。** 第 1 条要求"漂移变平"，而**漂移本身的成因还没查清**
（MLA 对折 + KDA 反转是两个独立现象），**在成因未知时把它当优化目标，
就是 §3 末尾那条"范数增长比不是可优化的目标"的负结果提前发生**。
选 2 只花一个 run，答一个确定能答的问题。

**原先记录的两层风险仍然有效，且第三个区间证实了第二层**：
（a）线性假设未验证——Muon 把更新正交化，更新范数主要由 lr 和形状决定而非梯度大小，
**所以线性假设在 Muon 下比在 AdamW 下更可能成立，但这是推理不是实测**；
（b）**3.589 是 500M/L32/24KDA+8MLA 上读的，臂跑在 200M/L12/9KDA+3MLA 上**——
现在连同一个 run 内部、相邻两个区间都能从 3.43 跳到 7.96，
**跨规模不变的假设更没有依据了。**

**预注册读数——按上面的判断，只留第 2 条为硬判据**：
1. ~~**KDA/MLA 范数增长比收敛到 ~1**（判据：落在 **[0.8, 1.25]**）~~
   **作废**：靶不固定（同一 run 内 3.43 → 7.96）。**比值仍然记录并逐区间打印**，
   但它是观察量不是判据。
2. **val loss 不劣于 A 臂**（差 < 0.02 nat 门）——**唯一的胜负判据**。
   **读的是"KDA 块用更低 lr 会不会掉质量"，这个问题有固定靶、不依赖线性假设。**

**第 1 条在 L=12 上的统计基础弱得多，而失效方式和我上一版写的不是同一个**：
`ckpt_health.py` 的 MLA 中位数在 L=32 上取自 **8 层**，在 L=12 上只有 **3 层**。
**上一版写"一层异常就能把整组挪走"——实测这是错的**，跑 `robust_z` 验了：

```
n=3，一层跑飞 (1.01, 1.02, 1.60):  z = [-0.7, 0.0, 39.1]  → 抓到
n=3，两层散开 (1.01, 1.20, 1.50):  z = [-0.67, 0.0, 1.06] → 漏掉
```

**单个离群点在 n=3 上抓得很好**（中位数和 MAD 都由另两层定，离群点动不了它们）。
**真正的盲区是"两层一起散开"**：三个点里有两个偏离，中位数就落在偏离的那一侧，
**MAD 变成偏离量本身**，于是 z 全部塌到 1 附近，没有一层过 3σ。
n=8 下同样的两层散开是 max z 3300（六个正常层把 sigma 钉住），**n=3 下是 1.06**。

**所以预注册的这一条不是"区间偏乐观"，是"3σ 判据在 n=3 上对某一类形状完全失灵"。**
**R3-lr 判定时必须打印三个 MLA 层各自的比值，不能只看中位数或 flag 数**——
**零 flag 在 n=3 上不构成"没有异常"的证据**。判据改为：
**三层比值的极差超过 25% 就按"增长比没收敛"处理**，不管有没有 flag。

**两种结果各触发什么**：
- **val loss 不劣（差 < 0.02 nat）** → **KDA 块可以吃 0.28 倍 lr 而不损失质量**，
  这本身就是可写进 recipe 的一条，300M 采用；**并把比值的三区间漂移落成 fact**
  （现在只有 b0-10 的读数，没有进 facts）。
- **val loss 变劣 ≥ 阈值** → 不采用，**并且"范数增长比是个该被等化的量"这个前提被否掉**——
  那是有价值的负结果，它省掉后续所有基于这个比值的调参。
- **比值在 B 臂下仍然漂移**（预期会）→ **不算失败**，因为它已不是判据；
  记录漂移形状，留给"成因是什么"这个单独的问题。

## 4. R1：KDA:MLA 比例 —— **已预注册，延后，不占这四个 run**

**为什么延后（fb 裁定，理由是空白硬度不是重要性）**：§2 的 OFF 臂在任何深度都没有
配对读数，§3 有 3.4–3.7× 的实测动机，**（1）两者都没有**——现状 3:1 已有 20B token
的运行记录，而"更稀疏会不会更好"今天只是猜想，没有任何读数指向它。
**下面整节保持可跑状态**，等 R2/R3 的结论回来再排。

**问的数**：val loss（同 mix、同 token、同 seed）。
**A 臂 = 现状 3:1** = B0（3 MLA + 9 KDA，MLA 在 2/6/10）。
**B 臂 = 5:1**（2 MLA + 10 KDA）——**比现状稀疏一档，L=12 上唯一的整数选项**。
**C 臂 = 1:1**（6 MLA + 6 KDA，交替）。

**三个点而不是两个**：5:1 和 1:1 在现状两侧各一档，
**三个点能区分"单调"和"3:1 已是内点最优"，两个点不能**。
**所以将来重启这一问时，只加 B 臂是半个实验**——它答不出 3:1 是不是内点最优。

**sigma 复用有条件**：B0/B0-seed2 定出的 sigma 是 **L12 + AttnRes ON + 单一 base lr**
下的。**若 R2 判 OFF 胜、或 R3 判分 lr 胜，基线配置就变了，这一问要重新定 sigma**——
届时 R1 的成本是 2 个 run（新基线 + 新 seed）加 2 个臂，不是 2 个臂。

**两种结果各触发什么（保持预注册状态）**：
- **5:1 优于 3:1 ≥ 阈值** → p300m 用同比例（**按 L=18 重排 MLA 位置和个数，
  不照抄 4/18**，见 §1 三），且 R3 的 lr 系数按新的 KDA 块数重算。
- **5:1 不优，或 1:1 优于 3:1** → 保持 3:1，**把"MLA 密度不是当前瓶颈"写进 facts**。
- **三臂都在阈值内** → 比例不敏感，**取最省的**（KDA 每块参数更多，则 1:1 更省），
  理由记进 fact。

## 4.5 ~~open 项~~ **已关闭：KDA 门的饱和判据，两个区间两次为负**（fb 定判据，1e 裁定关闭 2026-09-02）

**判据（不改代码，只观察）**：每头报**非饱和区宽度** = 使 `0.05 < sigmoid(exp(A_log)·g) < 0.95`
的 `g` 区间宽度。闭式：`width = 2·ln(19) / exp(A_log)`。**中位数与宽度一起看。**

**饱和本身不算坏**（fb）：一个头学成硬开关（记/忘二值）是 delta rule 里已知的可用形态。
**坏的信号只有两个**：
1. **饱和头数在第二区间单调增加**
2. **整层宽度中位塌缩**

### 第一次读的结果：两条都是负的

| | step500 | step832 | 判断 |
|---|---|---|---|
| 饱和头数（width < 0.2） | **0/72** | **1/72** | 一个头，无趋势 |
| 整层宽度中位 | **4.998** | **8.669** | **上升 73%，不是塌缩** |
| 最窄头 | 0.932 (L1) | **0.089 (L1)** | 就是 A_log=4.19 那个 |

**唯一饱和的那个头在 L1，而 L1 其余七个头的宽度都变宽了**（wmed 4.559 → 6.515）。

**为什么宽度在升而 A_log max 也在升——这一条值得记**：
`width = 2·ln(19)/exp(A_log)`，**所以宽度由中位 A_log 驱动，而中位是下降的**。
最大值与中位数反向移动，宽度中位跟随中位数。
**我上一版只看了 max，就把整件事读成了"门在饱和"**——
按 fb 的判据实测，**层的整体方向是反的**。

### 第二次读的结果：两条又都是负的 → 关闭（step1000→1500，续训后干净的 500 步）

| | step1000 | step1500 | fb 的判据 | 判断 |
|---|---|---|---|---|
| 饱和头数（width < 0.2） | **1/72** | **1/72** | 单调增 → 坏 | **没有增** |
| 整层宽度中位 | **9.121** | **11.219** | 塌缩 → 坏 | **又宽了 23%** |
| 最窄头 | 0.0696 | **0.0200** | —— | 仍是同一个头 |
| A_log max（`exp()`） | 4.44（84.6） | 5.69（295.2） | —— | 仍在涨 |

**九个 KDA 层的宽度中位，每一层都变宽**：
`0: 11.40→14.46, 1: 7.29→8.59, 2: 9.98→13.01, 4: 8.72→11.22, 5: 11.94→15.33,
6: 8.67→9.90, 8: 9.75→11.85, 9: 6.38→7.31, 10: 10.34→12.52`。

**关闭的理由是判据本身给的，不是"看着还行"**：fb 定的两条坏信号，
**在两个独立区间上各测一次、四个读数全部为负**，而"门在饱和"这个读法所依赖的
唯一上涨量（A_log **max**）与宽度中位**持续反向移动**——
第一区间 4.998→8.669，第二区间 9.121→11.219，两次都是变宽。
**同一个头（L1 那个）在两个区间里都是唯一饱和的**，没有第二个头加入。

**这条也是"只看 max 就下结论"这个错法的完整闭环**：我第一版把它读成"门在饱和"，
按判据实测方向是反的；第二个区间再测一次，方向还是反的。
**判据比直觉多活了两轮，所以判据留在这页，作为下一个人重开这一问时的入口。**

**`g` 的 token 级分布不再是必读项**——它原本是用来分辨"饱和后能否还区分距离"的，
而两个区间都没有饱和趋势，所以那个问题现在没有被指向的证据。
**要重开这一问，先要有一个区间给出"饱和头数增加"或"宽度中位塌缩"。**

### 事实（不改代码）

- **`lower_bound=-5.0` 是 fla 的默认值**，`model.py:137` 显式传的也是它。
  `fla/ops/kda/gate.py:69` 里它是 sigmoid 的乘数，**同时限住两端**：
  `g_out ∈ [-5, 0]`，每 token 衰减恒在 `[exp(-5), 1] = [0.0067, 1]`。
  **不存在"衰减无界"的风险。** 改它等于改架构，而没有任何读数说 −5.0 是错的。
- **NoPE 下位置信息全在 KDA 状态里**（fb 指出），所以衰减过快直接吃位置。
  **但按上一条衰减有下界**；要盯的是**饱和后能否还区分不同距离**，那要读 `g` 而不是 `A_log`。

## 4.6 embedding 范数持续放大：**共性，不是 200M 的配置问题**（读数，1e 重裁 2026-09-02）

**先给结论**：L12 和 L32 **两个规模都在涨**，所以这不是 200M 特有的毛病；
但 **L12 的倍数是 L32 的 2.4 倍**，所以"共性"不等于"同量级"。

| 规模 | 区间 | `tok.weight` 范数 | 倍数 |
|---|---|---|---|
| 200M / L12 | step1000→1500 | 5799.64 → 8171.73 | **1.4090×** |
| 200M / L12 | step1500→2000 | 8171.73 → 11673.19 | **1.4285×** |
| 500M / L32 | step2000→2500 | 10362.28 → 12094.60 | **1.1672×** |
| 500M / L32 | step2500→3000 | 12094.60 → 14226.24 | **1.1762×** |

**两个规模各自两个区间都稳定**（L12 1.409/1.429，L32 1.167/1.176），两边都 `tied=True`、
32,832 行。同期层：KDA 1.101×、MLA 1.036×，**embedding 比最快的层类还快 1.30 倍**。

**均匀放大，不是几行跑飞**（L12 step1500→2000 逐分位）：

| | step1500 | step2000 | 倍数 |
|---|---|---|---|
| 行范数中位 | 46.00 | 65.37 | 1.4209× |
| 行范数最大 | 60.85 | 86.37 | 1.4194× |
| top-1% 均值 | 55.42 | 77.59 | 1.3999× |
| 整矩阵 | 8171.7 | 11673.2 | 1.4285× |
| **最小行** | **0.602516** | **0.602516** | **1.0000×** ← padding 行完全冻结 |

**每个分位数同一个 1.42×。** 所以机制不是"个别 token 被反复更新"。

### 机制：读到的分组与 1e 假设的**两处都不同**

1e 的假设是"无 wd 的 tied embedding 在 lr 1e-2 下均匀指数放大"。**读 `train.py` 实测**：

| | 1e 假设 | 代码实际 | 出处 |
|---|---|---|---|
| 优化器 | Muon 或 AdamW，未定 | **AdamW** | `train.py:742` |
| lr | 1e-2 | **0.1（`embed_lr`）** | `train.py:251` |
| weight decay | 无（0） | **0.001（`embed_wd`）** | `train.py:253` |

**分组按名字**：`train.py:718-720` `if "tok" in n or "head" in n` → embed 组。

**两个修正都指向"放大更容易发生"而不是更难**：
- **embedding 的 lr 是 0.1，不是日志里那个 1.00e-02**。日志打的是
  `optimizers[0].param_groups[0]['lr']`（`train.py:2401`）= **Muon 的 lr**。
  **每一行 `lr 1.00e-02` 都不是 embedding 的 lr，embedding 跑在 10 倍的 lr 上。**
  **已记为 b0-14**（`docs/lessons/stop_window_2026-09-02.md`），下次 launch 时触发；
  `train.py` 在窗口前不动。
- **wd 不是 0，但它永不衰减**：`train.py:783` 的 wd decay-to-zero 有
  `isinstance(opt, Muon)` 守卫，**只对 Muon 生效**，所以 embed 组的 0.001
  全程恒定——而 muon_wd 0.10 会线性衰减到 0。**0.001 对 lr 0.1 来说约束极弱。**

**推断（标为推断，不是读数）**：AdamW 在 lr 0.1、wd 0.001 下对一个几乎每步都收到
同向梯度的 tied 参数，会给出接近恒定的相对增长率——**这与实测的"每区间同一倍数、
逐分位一致"吻合**。**验证方式**：读 optimizer state 里 embed 组的 `exp_avg`/`exp_avg_sq`
（ckpt 里有 `opt` 字段），看更新量是否稳定；或直接算 wd 项与更新项的比。**都是 CPU 可做的。**

### logits 尺度：**只给数，不下结论**（1e 明确要求）

**没有做 forward**——pod 上 CPU 跑 `load_checkpoint` 会撞 triton 的
`0 active drivers`，所以改为从权重算上界，不假装跑了一次前向：

| | step1000 | step1500 | step2000 |
|---|---|---|---|
| `norm.g` rms | 0.1710 | 0.1655 | **0.1708** |
| `‖hidden‖` ≈ `sqrt(d)·gain_rms` | 5.5 | 5.3 | 5.5 |
| head 行范数中位 | 32.63 | 46.00 | 65.37 |
| **logit 上界** ≈ 两者积 | **179** | **244** | **357** |
| softcap | 15.0 | 15.0 | 15.0 |

**这个上界不证明饱和，必须说清**：它是 Cauchy-Schwarz 的最坏情况（完全对齐），
真实 logit 是 `‖h‖·‖w_row‖·cos θ`，而高维下 `cos θ` 很小。
**它证明的是另一件事**：上界在两个区间里涨了 **1.99×**，而同期 `tok.weight` 范数涨 **2.01×**
——**上界完全跟着 embedding 走**（同一个张量，本该如此），而 **softcap 固定在 15**。

**真正 load-bearing 的一行是 `norm.g` rms 是平的**（0.171 / 0.166 / 0.171）。
**如果最终 norm 的 gain 在缩小以抵消 embedding 的放大，logit 尺度就是稳定的。它没有在缩小。**
所以"放大被下游吸收"这个可能性**没有得到支持**，但也没有被否证——**要否证得读真实 logit 分布，
那需要一次 forward，而这台 pod 的 CPU 路径跑不了**（需 GPU 或修 triton 的 CPU 回退）。

### 下一步：两种结果各对一个动作（1e 要求写清）

**实测落在"两个规模都涨"这一支**，所以：

- **不是 200M 配置的问题** → 不改 200M 的任何旋钮，不进 b0-12 的臂。
- **但 L12/L32 倍数差 2.4 倍（1.43 对 1.17）需要解释**，候选：深度（L32 的
  embedding 梯度被 32 层稀释）、`embed_lr` 是否随规模缩放、batch 与 token 数不同。
  **这三个都还没测，所以这是 open 项而不是结论。**
- **那个最便宜的验证做了，结果否证了它自己的假设**（1e 裁定后执行，CPU）：
  读两个规模 ckpt 的 embed 组 optimizer state（`opt[1]`，AdamW，**1 个参数**——tied 确认）：

  | | lr | wd | \|Adam 更新\| 均值 | wd 项均值 | **更新/wd** |
  |---|---|---|---|---|---|
  | L12 step2000 | 0.1 | 0.001 | 0.17248 | 1.591e-03 | **108×** |
  | L32 step3000 | 0.085 | 0.001 | 0.17419 | 1.939e-03 | **90×** |

  **更新/wd 只差 1.20 倍，而要解释的增长差是 2.43 倍——所以"wd 约束太弱"不是那个差的原因。**
  wd 在两个规模上都被压过两个数量级，**它解释"为什么会涨"，不解释"为什么 L12 涨得快"。**

### 2.43 倍分解成两项，而第二项不是任何人提过的候选

**Adam 的更新是尺度无关的**（量级 ≈ lr），所以每步相对增长 ≈ `lr·|upd|/|w|`：

| | `lr·\|upd\|/\|w\|` | 500 步若完全同向 | **实测** | **有效同向率** |
|---|---|---|---|---|
| L12 | 0.010843/步 | **6.42×** | 1.4285× | **7.9%** |
| L32 | 0.007636/步 | **4.82×** | 1.1762× | **4.6%** |

**两件事一起出来了**：

1. **更新绝大部分在互相抵消**：若每步同向，L12 一个区间该涨 6.4 倍，实测 1.43 倍
   ——**只有 7.9% 的更新落在增大范数的方向上**。所以这不是"参数被推着单向跑"，
   是一个随机游走里的小小净漂移。
2. **2.43 倍的分解**（乘积精确闭合）：

   ```
   lr 与权重尺度项   0.010843 / 0.007636 = 1.42×
   有效同向率项      0.079 / 0.046       = 1.71×
   乘积              1.42 × 1.71         = 2.43×   对着实测 2.43×
   ```

   **前一项是三个候选里的第二个（`embed_lr` 不随规模缩放）——它确实占了 1.42 倍，但只占一半。**
   **另一半是"L12 的更新跨步之间比 L32 更一致"，这一项此前没人提过，包括我。**

**这个乘积精确到两位小数，但我不把它当证明**：两个规模各一个点，**恰好闭合可能是巧合**
（§63 那条"两个反向误差相消"的邻居——一个乘积恰好对上，也可能是两个错抵消）。
**要立成机制，需要第三个点**：p300m/L18 的 embed 组做同一读数，
**预测有效同向率落在 4.6% 与 7.9% 之间且单调随深度下降**。
**这是一个可预注册的读数，不是一个已确认的机制。**

**若"深度稀释梯度"是第一个候选**，它现在有了一个具体形式：
**不是梯度变小（更新量两个规模几乎相同，0.172 对 0.174），而是方向跨步更不一致**。
候选一因此被改写而不是被排除——**32 层的 embedding 梯度来自更多层的叠加，方向更抖，
所以净漂移更小**。这是推断，验证方式同上。
- **若继续到 step3814**（还剩 3.63 个区间 × 1.43）→ 终值约 **42,600**，是 step1000 的 **7 倍**。
  **这个外推标为外推**：它假设倍数不衰减，而 L32 的倍数比 L12 低，
  **暗示倍数可能随训练进程下降**——所以 7 倍是上界不是预测。

### 机制定下来了：**放大来自 head 路径，不是输入路径**（读数 2026-09-03，零卡）

1e 提了一个 A/B（untie head，head 自己一组 AdamW lr≈0.0035）来测"放大是否来自 head 以
lr 0.1 训练"。**这个问题不用上卡就能答**，因为 tied 矩阵里已经有三种行，各自只吃一条路径：

| 行区间 | 作为输入 id | 作为 CE 目标 | 在 logit 切片里 | 吃到的梯度 |
|---|---|---|---|---|
| `[0, 32773)` | 会 | 会 | 在 | **输入 + head** |
| `[32773, 32784)` | **从不** | **从不**（`vocab_real` 以上） | **在**（`head.weight[:vocab]`） | **只有 head** |
| `[32784, 32832)` | 从不 | 从不 | **不在**（padded 之外） | **无** |

**step2500→3000 实测**（`ckpt_p200m_4b_0902.pt`，CPU 读权重）：

| 行类 | step2500 | step3000 | 倍数 |
|---|---|---|---|
| 只有 head 路径（11 行，逐行完全一致） | 13.2399 | 13.8540 | **1.0464×** |
| 输入+head（中位） | 76.6132 | 90.2697 | **1.1783×** |
| 无路径（48 行） | —— | —— | **max abs delta 恰好 0** |

**三件事一次落地**：

1. **无路径的 48 行 delta 恰好为 0**，证明这套读法本身没有噪声底 ——
   一个"没在动的东西"读出来就是 0，不是 1e-8。
2. **只有 head 路径的 11 行也在涨**（1.0464×），所以 **head 路径确实在放大 embedding**，
   1e 的方向是对的。
3. **但它只占 27.6%**：增长要按对数分（放大是乘性的），`ln1.0464 / ln1.1783` = **27.6%**。
   **剩下 72.4% 来自输入路径。** 所以 "untie + 给 head 低 lr" 最多切掉不到三成，
   **不会消掉放大** —— 而 A/B (1) 若只看总范数会被读成"改动几乎无效"。
   （**别用 `1.0464/1.1783` = 88.8%**，那是两个倍数的比，不是任何一条路径的份额；
   我第一版写的 26% 就是这么算错的。）

**11 行逐行完全一致（13.239851 → 13.853963，11 个相同数字）另有含义**：它们初始化后
从未被任何 token 区分过，所以一直是同一个向量，**说明这条 head 路径的梯度对这些行是
共同项**（softmax 分母那部分），不是 per-token 信号。**这也是 §4.6 "逐分位同一个倍数"
的机制**：一个所有行共享的乘性项。

**对 A/B (1) 的结论**：**问题已答，不必上卡**。要测的话它的读数应该是
"head-path-only 行是否停止增长"（预期是），**不是总范数**（预期只降不到三成）。
**省下 4 卡×500 步。**

**A/B (1) 还有一个必须先说的代价**：**我们的 head 是 tied 的**
（`model.py:322` `self.head.weight = self.tok.weight`，一个张量）。untie 不是换个 lr，
是 **+33.6M 参数（32832×1024）在 200M 上就是 +17%**。所以那个 A/B 不是等参数量的，
**它同时动了容量和 lr**，两个都影响 loss —— 单臂读不出是哪一个。要做就得三臂
（tied / untied-同lr / untied-低lr），成本 3 倍。**在上面这条零卡读数之后，不值得。**

### 顺带一条观察，**不立读数**（1e 裁定 2026-09-02）
**MLA layer 11 的 z=+16.21**（ratio 1.0536 对同类中位 1.0364）**不报为异常**。
   这正落在 `ckpt_health.py --selftest` 记录的 **n=3 盲区的另一面**：三个 MLA 里一个偏离时，
   中位数和 MAD 都由另两个定，所以 z 会飙到几十而**绝对差只有 1.7%**。
   KDA layer 2 的 z=−3.07（n=9）同理，绝对差 2.8%。
   **在 n=3 上，z 的量级不是严重程度**——要判 MLA 层异常，看的是绝对比值而不是 z。

## 5. 顺序与依赖

**四个 run 全部串行，排在 `p300m_6b_0902` 之后**，不与生产 run 抢卡。
**顺序是 B0 → B0-seed2 → R2-off → R3-lr**（R2 先于 R3，fb 同意 2026-09-02）。

1. **B0**：三问的共用基线。**它同时是 §3 那个 0.28 的前置**——
   落盘后先用 `scripts/ckpt_health.py` 在 L=12 上重读 KDA/MLA 增长比，
   **不在 3.0–4.2 之间就先改系数再跑 R3-lr**（见 §3 第二层风险）。
2. **B0-seed2**：定 sigma。**在它之前，任何"胜负"都读不出来**，
   所以这不是准备工作，而是让后两个 run 可读的必要条件。
3. **R2-off**：只依赖 sigma，不依赖 B0 的范数读数，**所以它排在 R3 之前**。
4. **R3-lr**：依赖 sigma 和 B0 的 L=12 增长比两者。

**R2 和 R3 之间没有依赖，但顺序仍不能换**：R2 若判 OFF 胜，
**基线定义就变了**（块少一路深度注意力，KDA/MLA 的范数占比要重读），
那时 R3 的 A 臂应当是 OFF 配置。**先跑 R2 使这个分支能被发现；先跑 R3 则发现不了**——
它会安静地对着一个可能被淘汰的基线出结论。

**这个顺序里没有 R1**：它延后（§4），且**将来重启时不能直接复用这批 sigma**，
理由在 §4。

## 6. 作废条件

- **块布局变化**：§0 的 3:1 和 MLA 位置来自 500M 的 step2500（L32）；
  **实验层是 L12，布局是 3 MLA + 9 KDA**。若 p200m 的布局改了，§2/§3/§4 的臂全部重定义。
- **3.4–3.7× 这个区间本身已经作废**（step3000 读到 7.96×，见 §3）。
  R3-lr 的动机改为"KDA 块能不能吃更低 lr 而不掉 val loss"，**那一问不依赖比值稳定**。

- **【这条已经触发，2026-09-02 13:0xZ，而且比预期早】** 上一条写的是"若 B0 在 L=12
  上读到的比值接近 1，0.28 就没有来源了"。**不必等 B0：p200m 自己就是 L12，实测 1.63×。**
  `ckpt_p200m_4b_0902` step500→`interrupt.step832`（run 停在 832，**没有平的
  `.pt.step832`**——本页和 fact 起初都写了这个不存在的名字，1e 2026-09-02 更正），
  9 KDA + 3 MLA：KDA 中位 **1.2069**、
  MLA 中位 **1.1270**，**比值 1.63×**，对着 500M/L32 的 3.43×–7.96×。
  （已进 `facts/efficiency.json#eff.kda_mla_growth_ratio_l12`，boundary 写明
  **单窗、warmup 后第一窗、不作系数来源**——44 复核指出这个数原先只活在
  commit message 和本页里。）
  **R3-lr 要等化的那个差距，在它要跑的规模上只有 500M 的一半到四分之一。**
  裁定请求（**我不自裁自己的实验**）：要么撤 R3-lr（1.63× 不值一个 run），
  要么保留 §3 已改过的读数（只看 val loss），**现在多一条理由：靶不仅不固定，而且变小了**。
  **0.28 这个数无论如何都作废了**——它是从 3.589 反推的，而 L12 的比值是 1.63，
  等化系数应为 **0.61**（若仍走等化路线，且线性假设仍未验证）。

- **500M 的 MLA layer 31 发散在 L12 上也没有复现**：最后一个 block 均值 **2.266**，
  对着 layer 7 的 2.259、layer 3 的 1.638，**在同类范围内**；L31 当时是另外七层中位数的
  **2.59 倍**、且高过 KDA 中位数。**那是 L32 特有现象，不是架构现象**，
  所以它不再是 R2-off 的顺读项（改为下面这条）。

- **【新增观察，500M 上没有】A_log 高端在 200M 上快速攀升。**
  KDA layer 1 的 A_log max **1.844 → 4.188**（332 步，`exp()` **6.3 → 65.9，10.4 倍**），
  而该层同时是两个 weight-norm flag 之一（ratio 1.348，z +3.17）。
  **500M 的 A_log max 在 step2500→3000 持平**（+1.4688 未变）。
  **均值下移（0.148 → −0.168）而最大值上移**：每头衰减率的分布在**散开**，不是平移。
  ~~`model.py:137` 只传 `lower_bound=-5.0`，**低端有夹、高端无界**。~~
  **这句错了，我读了 fla 源码后自己推翻的（2026-09-02）。**
  `fla/ops/kda/gate.py:69`（`safe_gate=True` 走的那条）是：

  ```python
  g = lower_bound * F.sigmoid(A_log.view(H, 1).exp() * g)
  ```

  **`-5.0` 是一个 sigmoid 的乘数，不是一个下夹**——所以 `g_out ∈ [-5, 0]`
  **对任何 A_log 都成立，两端都有界**。A_log = 4.19 还是 400，值域一样。
  **而且 `exp(A_log)` 根本不是衰减率**：它是 sigmoid 内部的斜率。
  我上一版报的"exp 6.3 → 65.9，衰减涨 10.4 倍"**把一个斜率读成了一个速率**。

  **A_log 变大真正做的事是让门饱和**：`exp(A_log)` 从 1.25 涨到 66，
  使门处于非饱和区的 `g` 区间宽度从 **4.73 塌到 0.089**（判据：0.05 < sigmoid < 0.95；
  闭式 `2·ln(19)/exp(A_log)`）。**我上一版写的 "2.00 → 0.08" 里那个 2.00 是错的**：
  我的数值扫描只扫了 `g ∈ [-1, 1]`，宽的那一端被扫描范围自己截断了——
  **量出来的是我的扫描窗口，不是那个宽度**。闭式与数值扫描（±5 范围）现在一致。
  **门变成阶跃**——每 token 的衰减几乎只取 `exp(0) = 1`（全记）或 `exp(-5) = 0.0067`（全忘），
  中间没有。**所以这不是"记忆窗过短"的风险，是"门失去连续调节能力"的风险**，两者要查的东西不同。
  **混淆未排除**：step 500–832 是 warmup（300 步）之后的第一个窗口，
  而 500M 的读数在 step 2500 之后。**所以这是观察不是结论**，
  分辨"早期瞬态"与"持续发散"需要 200M 续训后的第二个区间。
  **按 fb 的判据（少数头 vs 整层中位）实测的结果：是分化，不是发散。**
  KDA layer 1 的八个头，step500 → step832：

  ```
  -0.16 → -0.47   +0.15 → -0.18   +0.16 → -0.12   +0.26 → -0.10
  +0.67 → +0.22   +0.67 → +0.42   +1.36 → +2.06   +1.84 → +4.19
  ```

  **六个头下移，两个头上冲；中位数 +0.670 → +0.220，是下移的。**
  fb 的判据是"整层中位上移才算发散"——**中位在降，所以按这条判据不是发散**。

  **但结合上面的饱和分析，判据要加一条**：那两个上冲的头
  （`exp(A_log)` = 7.85 和 66.0）**门已经饱和**，非饱和区宽度 0.08。
  **"分化"和"两个头的门坏掉"在 A_log 的中位数上长得一样**——
  下一次读要按头报非饱和区宽度，不只报中位数。

  **R2-off 的顺读项改成这一条。**
- **72K 是稳态读数，短 run 的有效吞吐低于它**：1B = 954 步，
  启动成本（`build_mix`、compile 首步、3b 正在量的缓存读）摊在 28.5 分钟墙钟上。
  **B0 落盘后用真实墙钟重算 §1 的绝对卡时。**
  **但启动秒数不会翻转 §1 一的结论**——推导见 §1 一末。
- **0.02 nat 门未定**：`sigma` 是 500M/0.2B 的读数，**200M/1B 的 seed 方差没测过**；
  B0 与 B0-seed2 出结果之前，所有"可读"判断都是暂定的。
- **200M/300M 阶梯变化**：§1 的验收标准挂在这个阶梯上，阶梯改则验收改。
- **`eff.fb_mfu` 的 batch 32 已判为未经测量**（fb 2026-09-02）：那条 fact 的 source 是
  briefing，b32a1 在 `eff.microbatch_32_oom` 里实测 OOM。**本页现在用 72K/b16a2。
  若 fb_mfu 被修正或撤下，§1 的绝对卡时要重算，但 2.00 倍那个结论不受影响**——
  它由 token 比 2:1 决定，不由吞吐值决定。

**一条我自己犯的、值得单独记的**：我把 `eff.fb_mfu` 的「batch 32」当成一个被测过的配置，
然后拿它去给一次 OOM 归因——**结论是「record_shapes 吃掉了显存」，而真相是 batch 32
从来装不下**（同一个 93.8/95.2 GB 出现三次，其中一次进程里根本没有 profiler）。
**一条 fact 的 source 是「briefing」时，它记的是有人说过的话，不是有人量过的数**；
拿它作为参照物去判别人的测量，等于用一个未验证的前提去否证一个实测。
（`docs/standards/writing.md`：推导继承前提的状态。）
- **四个 run 的上限被放宽**：R1 是第一个该恢复的（§4），
  但**要连带新基线和新 seed 一起排**，不是只加两个臂。
