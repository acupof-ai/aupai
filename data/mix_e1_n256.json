{
 "_comment": "Experiment 1 arm n256 (runs/prereg.jsonl#conversion_rate_0905). GENERATED by scripts/e1_build_arm_mixes.py -- do not hand-edit a weight; rerun it. Resumes ckpt_b0_headmix_armA.pt, whose row_cursor sums to 244160 rows, so total_tokens carries cursor + this arm's 32000 rows and build_mix's weights are shares of the 32000-row remainder.",
 "total_tokens": 1131151360,
 "total_rows": 276160,
 "seq": 4096,
 "anneal_frac": 0.0,
 "warmdown": 0.1,
 "_budget_rationale": "docs/lessons/mix_500m_rationale.md",
 "_code_supply_tokens_at_generation": 8744830156,
 "_warnings": [],
 "_untrusted_supply": {},
 "_launch_blocked": [],
 "domains": {
  "math_owm_stage2": {
   "weight": 0.20996063426930642,
   "epochs": 1,
   "anneal": 0.20996063426930642,
   "role": "math/reasoning. Second-largest: the 16B readout put math and reasoning on the steepest part of their curves, and code and math share the symbolic-structure transfer this size can still absorb. Weight is mix_200m_8b's x 0.7944687035156361 = (1 - w_S - w_P)/1.00000005851, so every ratio between two natural domains is exactly the control's while this arm's total is 1.",
   "weight_decimals": 7,
   "rows_from_weight_at_runtime": 434557,
   "want_tokens": 1779945472,
   "supply_tokens_one_epoch": 6513304690,
   "epochs_fractional": 0.2733,
   "pool_rows_estimated": 1588494,
   "cursor_used_rows": 0,
   "cap_covers": 434557,
   "epochs_pool_measured": true,
   "epochs_pool_source": "MEASURED from the token cache: 1,588,494 drawable rows (rows = cache.numel()//(seq+1), minus n_val), read 2026-09-01",
   "fingerprint": "1e687e4b5ce37598",
   "fingerprint_source": "read from data/corpus/math_owm_stage2/build_corpus_stats.json",
   "epoch_cap_note": "epochs 1 = ceil((0+434557)/1588494). PROVISIONAL: the pool is estimated as stamp_tokens//(seq+1) minus n_val, not measured from a token cache. Re-derive against the real cache before launch -- a stamp gives tokens, a pool is packed rows."
  },
  "en_c4_stage2": {
   "weight": 0.1292066070020902,
   "epochs": 1,
   "anneal": 0.1292066070020902,
   "role": "English general. Carries the natural-language competence code docstrings and problem statements are written in; below ~15% the prose side of a code model degrades before the code side does. Weight is mix_200m_8b's x 0.7944687035156361 = (1 - w_S - w_P)/1.00000005851, so every ratio between two natural domains is exactly the control's while this arm's total is 1.",
   "weight_decimals": 8,
   "rows_from_weight_at_runtime": 267420,
   "want_tokens": 1095352320,
   "supply_tokens_one_epoch": 2403694865,
   "epochs_fractional": 0.4557,
   "pool_rows_estimated": 581073,
   "cursor_used_rows": 0,
   "cap_covers": 267420,
   "epochs_pool_measured": true,
   "epochs_pool_source": "MEASURED from the token cache: 581,073 drawable rows (rows = cache.numel()//(seq+1), minus n_val), read 2026-09-01",
   "fingerprint": "05e0fc6f14704056",
   "fingerprint_source": "read from data/corpus/en_c4_stage2/build_corpus_stats.json",
   "epoch_cap_note": "epochs 1 = ceil((0+267420)/581073). PROVISIONAL: the pool is estimated as stamp_tokens//(seq+1) minus n_val, not measured from a token cache. Re-derive against the real cache before launch -- a stamp gives tokens, a pool is packed rows."
  },
  "cot": {
   "weight": 0.06410925479584251,
   "epochs": 4,
   "anneal": 0.06410925479584251,
   "role": "chain-of-thought. THE WEIGHT IS A CEILING, NOT A TARGET, so it is\nderived (None) rather than typed: cot's supply is 0.424B, the smallest of the eight, so at 9% of 20B it draws 4.24 epochs and crosses Muennighoff's 4-epoch line while 8% draws 3.77. Held at the largest value that stays under. WHAT THIS WEIGHT IS NOT: it is not derived from this role's value to the objective, because nothing measures that. An earlier draft moved a point out of here by comparing two roles' per-token nat/B, which docs/standards/training_loop.md section 6 -- which I wrote -- forbids: transfer inflates every such rate by an unmeasured, role-dependent amount, so the ordering across roles is not known to hold. That sentence is withdrawn, and _refuse_cross_role_rate now refuses it mechanically. The withdrawn numbers are not quoted here because the refusal cannot tell a citation from a quotation of one, and it should not try -- an exemption for 'but I am disowning it' is the hole the next violation goes through. Weight is mix_200m_8b's x 0.7944687035156361 = (1 - w_S - w_P)/1.00000005851, so every ratio between two natural domains is exactly the control's while this arm's total is 1.",
   "weight_decimals": 7,
   "rows_from_weight_at_runtime": 394016,
   "want_tokens": 1613889536,
   "supply_tokens_one_epoch": 424056227,
   "epochs_fractional": 3.8058,
   "pool_rows_estimated": 98504,
   "cursor_used_rows": 0,
   "cap_covers": 394016,
   "epochs_pool_measured": true,
   "epochs_pool_source": "MEASURED from the token cache: 98,504 drawable rows (rows = cache.numel()//(seq+1), minus n_val), read 2026-09-01",
   "fingerprint": "388496b76ed9bf88",
   "fingerprint_source": "read from data/corpus/cot/build_corpus_stats.json",
   "epoch_cap_note": "epochs 4 = ceil((0+394016)/98504). PROVISIONAL: the pool is estimated as stamp_tokens//(seq+1) minus n_val, not measured from a token cache. Re-derive against the real cache before launch -- a stamp gives tokens, a pool is packed rows."
  },
  "textbook_30b": {
   "weight": 0.08075402751350154,
   "epochs": 1,
   "anneal": 0.08075402751350154,
   "role": "textbook/instructional. The Phi-1 'textbook quality' arm: curated instructional prose is the ingredient that paper credits, and this is the closest domain we have to it. 10% rather than 9% because cot's ceiling released a point and textbook is the domain the OBJECTIVE ranks next -- not because its measured nat/B is higher, which would be the cross-role comparison section 6 forbids. Weight is mix_200m_8b's x 0.7944687035156361 = (1 - w_S - w_P)/1.00000005851, so every ratio between two natural domains is exactly the control's while this arm's total is 1.",
   "weight_decimals": 9,
   "rows_from_weight_at_runtime": 167137,
   "want_tokens": 684593152,
   "supply_tokens_one_epoch": 1610210330,
   "epochs_fractional": 0.4252,
   "pool_rows_estimated": 388021,
   "cursor_used_rows": 0,
   "cap_covers": 167137,
   "epochs_pool_measured": true,
   "epochs_pool_source": "MEASURED from the token cache: 388,021 drawable rows (rows = cache.numel()//(seq+1), minus n_val), read 2026-09-01",
   "fingerprint": "3f237c5191cb8571",
   "fingerprint_source": "read from data/corpus/textbook_30b/build_corpus_stats.json",
   "epoch_cap_note": "epochs 1 = ceil((0+167137)/388021). PROVISIONAL: the pool is estimated as stamp_tokens//(seq+1) minus n_val, not measured from a token cache. Re-derive against the real cache before launch -- a stamp gives tokens, a pool is packed rows."
  },
  "chatml": {
   "weight": 0.005885447989704938,
   "epochs": 4,
   "anneal": 0.005885447989704938,
   "role": "ChatML-rendered chat. The corpus contains effectively none (<0.075% per domain) and the 200M round showed SFT installs only the canon it is fed -- relying on SFT to teach a prefix the base has never seen is a bet this project already lost once. THE SAME 160,414 CONVERSATIONS ALSO SIT IN chat_qa, and each domain draws ~4 epochs, so this content is seen 8-10 times in total (e1 found the overlap; fb ruled KEEP, 2026-09-01). The ruling is not that the repetition is harmless in general -- it is that what is being bought here is a FORMAT entering the distribution, formats are cheap to learn, and 1.5% combined is what that costs. What would change it: evidence that the model is reciting these conversations rather than absorbing the markup. Nobody has measured that. WEIGHT IS None BECAUSE IT IS NOT A JUDGEMENT: this domain is SUPPLY_CAPPED, so _ceiling_weight derives it from the measured supply and the 4-epoch line. A literal here would be a second copy of that arithmetic, free to drift from it -- and it did: the typed 0.0076 was sized for a supply of exactly 38.0M and survived the switch to a 2sf measurement that made it a coin flip. The cap is deliberate rather than regrettable: the job is to make a FORMAT in-distribution, and formats are cheap to learn. Fifteen epochs would buy memorisation of 39M tokens of QA content we do not want memorised, in order to teach a markup (fb 2026-09-01). Too few epochs is measurable and fixable next run; 15 is baked into the weights. Weight is mix_200m_8b's x 0.7944687035156361 = (1 - w_S - w_P)/1.00000005851, so every ratio between two natural domains is exactly the control's while this arm's total is 1.",
   "weight_decimals": 7,
   "rows_from_weight_at_runtime": 36172,
   "want_tokens": 148160512,
   "supply_tokens_one_epoch": 38995846,
   "epochs_fractional": 3.7994,
   "pool_rows_estimated": 9043,
   "cursor_used_rows": 0,
   "cap_covers": 36172,
   "epochs_pool_measured": true,
   "epochs_pool_source": "MEASURED from the token cache: 9,043 drawable rows (rows = cache.numel()//(seq+1), minus n_val), read 2026-09-01",
   "fingerprint": "52a6b427818c90ff",
   "fingerprint_source": "read from data/corpus/chatml/build_corpus_stats.json",
   "epoch_cap_note": "epochs 4 = ceil((0+36172)/9043). PROVISIONAL: the pool is estimated as stamp_tokens//(seq+1) minus n_val, not measured from a token cache. Re-derive against the real cache before launch -- a stamp gives tokens, a pool is packed rows."
  },
  "chat_qa": {
   "weight": 0.005762440400339612,
   "epochs": 4,
   "anneal": 0.005762440400339612,
   "role": "The same QA rows in plain 问：/答： form, rendered into its own directory. Two renders of one source, so each domain holds ~0.038B and each is capped independently -- the pair is ~1.5% combined, not each. THAT MEANS THE SAME 160,414 CONVERSATIONS ARE SEEN 8-10 TIMES across the two domains, which fb ruled acceptable on 2026-09-01 for the reason spelled out in chatml's entry: the purchase is two formats in-distribution, not two passes of content. Weight is mix_200m_8b's x 0.7944687035156361 = (1 - w_S - w_P)/1.00000005851, so every ratio between two natural domains is exactly the control's while this arm's total is 1.",
   "weight_decimals": 6,
   "rows_from_weight_at_runtime": 35416,
   "want_tokens": 145063936,
   "supply_tokens_one_epoch": 38187650,
   "epochs_fractional": 3.7987,
   "pool_rows_estimated": 8854,
   "cursor_used_rows": 0,
   "cap_covers": 35416,
   "epochs_pool_measured": true,
   "epochs_pool_source": "MEASURED from the token cache: 8,854 drawable rows (rows = cache.numel()//(seq+1), minus n_val), read 2026-09-01",
   "fingerprint": "42a4bedde020ae66",
   "fingerprint_source": "read from data/corpus/chat_qa/build_corpus_stats.json",
   "epoch_cap_note": "epochs 4 = ceil((0+35416)/8854). PROVISIONAL: the pool is estimated as stamp_tokens//(seq+1) minus n_val, not measured from a token cache. Re-derive against the real cache before launch -- a stamp gives tokens, a pool is packed rows."
  },
  "zh_web": {
   "weight": 0.024226290304394753,
   "epochs": 1,
   "anneal": 0.024226290304394753,
   "role": "Chinese web, capped by the objective. Was 10.955%, and that number came from the warehouse: 21.3B sitting on disk. The target is an English-language code model, and Chinese web prose is not an ingredient of it -- 3% is kept only so the tokenizer's Chinese side does not go cold. The first draft also cited this role's own-token nat/B and its rank among the seven; the rank is struck, because ranking roles by nat/B is the section-6 comparison, and the weight does not need it: the objective already excludes Chinese prose. 21.3B of supply is now irrelevant -- the clearest single case of composition following the target, not the stock. Weight is mix_200m_8b's x 0.7944687035156361 = (1 - w_S - w_P)/1.00000005851, so every ratio between two natural domains is exactly the control's while this arm's total is 1.",
   "weight_decimals": 7,
   "rows_from_weight_at_runtime": 50141,
   "want_tokens": 205377536,
   "supply_tokens_one_epoch": 21293403945,
   "epochs_fractional": 0.0096,
   "pool_rows_estimated": 5192316,
   "cursor_used_rows": 0,
   "cap_covers": 50141,
   "epochs_pool_measured": true,
   "epochs_pool_source": "MEASURED from the token cache: 5,192,316 drawable rows (rows = cache.numel()//(seq+1), minus n_val), read 2026-09-01",
   "fingerprint": "a0d44fc44a289d60",
   "fingerprint_source": "read from data/corpus/zh_web/build_corpus_stats.json",
   "epoch_cap_note": "epochs 1 = ceil((0+50141)/5192316). PROVISIONAL: the pool is estimated as stamp_tokens//(seq+1) minus n_val, not measured from a token cache. Re-derive against the real cache before launch -- a stamp gives tokens, a pool is packed rows."
  },
  "code_py_starcoder": {
   "weight": 0.2619461035141585,
   "epochs": 1,
   "anneal": 0.2619461035141585,
   "role": "code, Python only (ast.parse is both language ID and syntax filter). Phi-1: one language, 6B curated Python, HumanEval 50.6 at 1.3B. The largest single objective because the target is code. Split across two corpora in proportion to supply, so both draw the same 0.30 epochs; the split is a supply fact, the 34% is the objective. Weight is mix_200m_8b's x 0.7944687035156361 = (1 - w_S - w_P)/1.00000005851, so every ratio between two natural domains is exactly the control's while this arm's total is 1.",
   "weight_decimals": 9,
   "rows_from_weight_at_runtime": 542151,
   "want_tokens": 2220650496,
   "supply_tokens_one_epoch": 8744830156,
   "epochs_fractional": 0.2539,
   "pool_rows_estimated": 2139719,
   "cursor_used_rows": 0,
   "cap_covers": 542151,
   "epochs_pool_measured": true,
   "epochs_pool_source": "MEASURED from the token cache: 2,139,719 drawable rows (rows = cache.numel()//(seq+1), minus n_val), read 2026-09-01",
   "fingerprint": "e1a14839e11f3e6f",
   "fingerprint_source": "read from data/corpus/code_py_starcoder/build_corpus_stats.json",
   "epoch_cap_note": "epochs 1 = ceil((0+542151)/2139719). PROVISIONAL: the pool is estimated as stamp_tokens//(seq+1) minus n_val, not measured from a token cache. Re-derive against the real cache before launch -- a stamp gives tokens, a pool is packed rows."
  },
  "code_py_rp1t": {
   "weight": 0.012617944210661584,
   "epochs": 1,
   "anneal": 0.012617944210661584,
   "role": "code, Python only (ast.parse is both language ID and syntax filter). Phi-1: one language, 6B curated Python, HumanEval 50.6 at 1.3B. The largest single objective because the target is code. Split across two corpora in proportion to supply, so both draw the same 0.30 epochs; the split is a supply fact, the 34% is the objective. Weight is mix_200m_8b's x 0.7944687035156361 = (1 - w_S - w_P)/1.00000005851, so every ratio between two natural domains is exactly the control's while this arm's total is 1.",
   "weight_decimals": 6,
   "rows_from_weight_at_runtime": 26115,
   "want_tokens": 106967040,
   "supply_tokens_one_epoch": 421239303,
   "epochs_fractional": 0.2539,
   "pool_rows_estimated": 97722,
   "cursor_used_rows": 0,
   "cap_covers": 26115,
   "epochs_pool_measured": true,
   "epochs_pool_source": "MEASURED from the token cache: 97,722 drawable rows (rows = cache.numel()//(seq+1), minus n_val), read 2026-09-01",
   "fingerprint": "63c2b3ce0e7dd989",
   "fingerprint_source": "read from data/corpus/code_py_rp1t/build_corpus_stats.json",
   "epoch_cap_note": "epochs 1 = ceil((0+26115)/97722). PROVISIONAL: the pool is estimated as stamp_tokens//(seq+1) minus n_val, not measured from a token cache. Re-derive against the real cache before launch -- a stamp gives tokens, a pool is packed rows."
  },
  "s_inject_n256": {
   "weight": 0.20490625,
   "anneal": 0.20490625,
   "epochs": 1,
   "val_frac": 0,
   "weight_decimals": 12,
   "role": "S injection: 256000 document lines, 26867200 tokens in the <eos>-separated stream (256000 of them the per-document <eos> train.encode appends), packing to 6557 rows at seq+1=4097. Weight = 6557/32000 budget rows, COMPUTED by scripts/e1_build_arm_mixes.py rather than typed: the three previous hand-derived values were each wrong (P's mean read as S's 104, a division by seq instead of seq+1, and a missing <eos> per document). val_frac 0 because this domain's row count IS the measurement."
  },
  "p_format": {
   "weight": 0.000625,
   "anneal": 0.000625,
   "epochs": 1,
   "val_frac": 0,
   "weight_decimals": 12,
   "role": "P format control: 1000 document lines, 85434 tokens in the <eos>-separated stream (1000 of them the per-document <eos> train.encode appends), packing to 20 rows at seq+1=4097. Weight = 20/32000 budget rows, COMPUTED by scripts/e1_build_arm_mixes.py rather than typed: the three previous hand-derived values were each wrong (P's mean read as S's 104, a division by seq instead of seq+1, and a missing <eos> per document). val_frac 0 because this domain's row count IS the measurement."
  }
 }
}