{
 "chat": "built before filters_fp existed, 2026-08-31",
 "chat_qa": "VOID as of 2026-09-17 (fp_filters redefined to hash the compiled PATTERNS instead of the filter files' bytes; genB, fb ruling): both values in the pair below are BYTE-hash generation and are not comparable to a current stamp or to the live value, which now carries the p1- generation marker. The physical fact this entry records is UNCHANGED -- these domains were still built with the pod's weaker filters and nobody rebuilt them -- but the ruled-mismatch mechanism cannot forgive them any more, because it matches on fingerprint values that no longer exist in any generation. REQUIRED ACTION: a rebuild re-stamps each domain in the new generation and this entry is then deleted. Until then check_corpus_filters_fp reports these domains as UNMIGRATED debt, not as a mismatch. [was: 33462c13868a2194 -> 88ee503b38941bf4: ruled mismatch, facts/data_quality.json#dq.stage2_corpus_built_with_weaker_filters. Built with the pod's filters, which lacked five CCI3-HQ rules main added 2026-08-30. Measured residue 0.25% of the training corpus; fb ruled no rebuild 2026-09-01. Shrinks when the domain is rebuilt.]",
 "chatml": "VOID as of 2026-09-17 (fp_filters redefined to hash the compiled PATTERNS instead of the filter files' bytes; genB, fb ruling): both values in the pair below are BYTE-hash generation and are not comparable to a current stamp or to the live value, which now carries the p1- generation marker. The physical fact this entry records is UNCHANGED -- these domains were still built with the pod's weaker filters and nobody rebuilt them -- but the ruled-mismatch mechanism cannot forgive them any more, because it matches on fingerprint values that no longer exist in any generation. REQUIRED ACTION: a rebuild re-stamps each domain in the new generation and this entry is then deleted. Until then check_corpus_filters_fp reports these domains as UNMIGRATED debt, not as a mismatch. [was: 33462c13868a2194 -> 88ee503b38941bf4: ruled mismatch, facts/data_quality.json#dq.stage2_corpus_built_with_weaker_filters. Built with the pod's filters, which lacked five CCI3-HQ rules main added 2026-08-30. Measured residue 0.25% of the training corpus; fb ruled no rebuild 2026-09-01. Shrinks when the domain is rebuilt.]",
 "code": "built before filters_fp existed, 2026-08-31",
 "code_py_rp1t": "VOID as of 2026-09-17 (fp_filters redefined to hash the compiled PATTERNS instead of the filter files' bytes; genB, fb ruling): both values in the pair below are BYTE-hash generation and are not comparable to a current stamp or to the live value, which now carries the p1- generation marker. The physical fact this entry records is UNCHANGED -- these domains were still built with the pod's weaker filters and nobody rebuilt them -- but the ruled-mismatch mechanism cannot forgive them any more, because it matches on fingerprint values that no longer exist in any generation. REQUIRED ACTION: a rebuild re-stamps each domain in the new generation and this entry is then deleted. Until then check_corpus_filters_fp reports these domains as UNMIGRATED debt, not as a mismatch. [was: 33462c13868a2194 -> 88ee503b38941bf4: ruled mismatch, facts/data_quality.json#dq.stage2_corpus_built_with_weaker_filters. Built with the pod's filters, which lacked five CCI3-HQ rules main added 2026-08-30. Measured residue 0.25% of the training corpus; fb ruled no rebuild 2026-09-01. Shrinks when the domain is rebuilt.]",
 "code_py_starcoder": "VOID as of 2026-09-17 (fp_filters redefined to hash the compiled PATTERNS instead of the filter files' bytes; genB, fb ruling): both values in the pair below are BYTE-hash generation and are not comparable to a current stamp or to the live value, which now carries the p1- generation marker. The physical fact this entry records is UNCHANGED -- these domains were still built with the pod's weaker filters and nobody rebuilt them -- but the ruled-mismatch mechanism cannot forgive them any more, because it matches on fingerprint values that no longer exist in any generation. REQUIRED ACTION: a rebuild re-stamps each domain in the new generation and this entry is then deleted. Until then check_corpus_filters_fp reports these domains as UNMIGRATED debt, not as a mismatch. [was: 33462c13868a2194 -> 88ee503b38941bf4: ruled mismatch, facts/data_quality.json#dq.stage2_corpus_built_with_weaker_filters. Built with the pod's filters, which lacked five CCI3-HQ rules main added 2026-08-30. Measured residue 0.25% of the training corpus; fb ruled no rebuild 2026-09-01. Shrinks when the domain is rebuilt.]",
 "cot": "VOID as of 2026-09-17 (fp_filters redefined to hash the compiled PATTERNS instead of the filter files' bytes; genB, fb ruling): both values in the pair below are BYTE-hash generation and are not comparable to a current stamp or to the live value, which now carries the p1- generation marker. The physical fact this entry records is UNCHANGED -- these domains were still built with the pod's weaker filters and nobody rebuilt them -- but the ruled-mismatch mechanism cannot forgive them any more, because it matches on fingerprint values that no longer exist in any generation. REQUIRED ACTION: a rebuild re-stamps each domain in the new generation and this entry is then deleted. Until then check_corpus_filters_fp reports these domains as UNMIGRATED debt, not as a mismatch. [was: 33462c13868a2194 -> 88ee503b38941bf4: ruled mismatch, facts/data_quality.json#dq.stage2_corpus_built_with_weaker_filters. Built with the pod's filters, which lacked five CCI3-HQ rules main added 2026-08-30. Measured residue 0.25% of the training corpus; fb ruled no rebuild 2026-09-01. Shrinks when the domain is rebuilt.]",
 "en": "built before filters_fp existed, 2026-08-31",
 "en_c4_stage2": "VOID as of 2026-09-17 (fp_filters redefined to hash the compiled PATTERNS instead of the filter files' bytes; genB, fb ruling): both values in the pair below are BYTE-hash generation and are not comparable to a current stamp or to the live value, which now carries the p1- generation marker. The physical fact this entry records is UNCHANGED -- these domains were still built with the pod's weaker filters and nobody rebuilt them -- but the ruled-mismatch mechanism cannot forgive them any more, because it matches on fingerprint values that no longer exist in any generation. REQUIRED ACTION: a rebuild re-stamps each domain in the new generation and this entry is then deleted. Until then check_corpus_filters_fp reports these domains as UNMIGRATED debt, not as a mismatch. [was: 33462c13868a2194 -> 88ee503b38941bf4: ruled mismatch, facts/data_quality.json#dq.stage2_corpus_built_with_weaker_filters. Built with the pod's filters, which lacked five CCI3-HQ rules main added 2026-08-30. Measured residue 0.25% of the training corpus; fb ruled no rebuild 2026-09-01. Shrinks when the domain is rebuilt.]",
 "math": "built before filters_fp existed, 2026-08-31",
 "math_owm_stage2": "VOID as of 2026-09-17 (fp_filters redefined to hash the compiled PATTERNS instead of the filter files' bytes; genB, fb ruling): both values in the pair below are BYTE-hash generation and are not comparable to a current stamp or to the live value, which now carries the p1- generation marker. The physical fact this entry records is UNCHANGED -- these domains were still built with the pod's weaker filters and nobody rebuilt them -- but the ruled-mismatch mechanism cannot forgive them any more, because it matches on fingerprint values that no longer exist in any generation. REQUIRED ACTION: a rebuild re-stamps each domain in the new generation and this entry is then deleted. Until then check_corpus_filters_fp reports these domains as UNMIGRATED debt, not as a mismatch. [was: 33462c13868a2194 -> 88ee503b38941bf4: ruled mismatch, facts/data_quality.json#dq.stage2_corpus_built_with_weaker_filters. Built with the pod's filters, which lacked five CCI3-HQ rules main added 2026-08-30. Measured residue 0.25% of the training corpus; fb ruled no rebuild 2026-09-01. Shrinks when the domain is rebuilt.]",
 "textbook": "built before filters_fp existed, 2026-08-31",
 "textbook_30b": "VOID as of 2026-09-17 (fp_filters redefined to hash the compiled PATTERNS instead of the filter files' bytes; genB, fb ruling): both values in the pair below are BYTE-hash generation and are not comparable to a current stamp or to the live value, which now carries the p1- generation marker. The physical fact this entry records is UNCHANGED -- these domains were still built with the pod's weaker filters and nobody rebuilt them -- but the ruled-mismatch mechanism cannot forgive them any more, because it matches on fingerprint values that no longer exist in any generation. REQUIRED ACTION: a rebuild re-stamps each domain in the new generation and this entry is then deleted. Until then check_corpus_filters_fp reports these domains as UNMIGRATED debt, not as a mismatch. [was: 33462c13868a2194 -> 88ee503b38941bf4: ruled mismatch, facts/data_quality.json#dq.stage2_corpus_built_with_weaker_filters. Built with the pod's filters, which lacked five CCI3-HQ rules main added 2026-08-30. Measured residue 0.25% of the training corpus; fb ruled no rebuild 2026-09-01. Shrinks when the domain is rebuilt.]",
 "web_hq": "built before filters_fp existed, 2026-08-31",
 "wiki": "built before filters_fp existed, 2026-08-31",
 "zh_web": "VOID as of 2026-09-17 (fp_filters redefined to hash the compiled PATTERNS instead of the filter files' bytes; genB, fb ruling): both values in the pair below are BYTE-hash generation and are not comparable to a current stamp or to the live value, which now carries the p1- generation marker. The physical fact this entry records is UNCHANGED -- these domains were still built with the pod's weaker filters and nobody rebuilt them -- but the ruled-mismatch mechanism cannot forgive them any more, because it matches on fingerprint values that no longer exist in any generation. REQUIRED ACTION: a rebuild re-stamps each domain in the new generation and this entry is then deleted. Until then check_corpus_filters_fp reports these domains as UNMIGRATED debt, not as a mismatch. [was: 33462c13868a2194 -> 88ee503b38941bf4: ruled mismatch, facts/data_quality.json#dq.stage2_corpus_built_with_weaker_filters. Built with the pod's filters, which lacked five CCI3-HQ rules main added 2026-08-30. Measured residue 0.25% of the training corpus; fb ruled no rebuild 2026-09-01. Shrinks when the domain is rebuilt.]"
}
