{
  "facts": [
    {
      "id": "v41.size_derivation_0910",
      "value": "Small-scale CSA2 config derived for the p1 model (task ae-2, V4.1 Step 0; corrected after 3b's PR #213 review): L=12, d=1024, heads=8, MoE 48/top-3/1-shared with expert_ffn=1728 (ffn_hidden=6912 under the existing equal-active parity), giving 352.3M active params/token (33.6M tied embedding + 318.8M layers) and 3.22B total (name 3.2b-a352m-e48). Per-layer attention params per paper Fig. 4: only Full computes Main K/V -- S = 4d^2 (Q + SWA K/V/O), F = 7d^2 + idx, X = 5d^2 + idx, R = 5d^2 (Main KV and Top-K reused). CSA2: m=8 tokens/entry, top_k=64, n_win=128, indexer 4 heads x 64 dim (latent 256 = d//4). Mode placement composes with PR #216 (0e-2): n_swa_only_layers=2 owns the first-two pure-SWA placement in HybridLM's kind loop, and csa2_modes=\"F,R,R,R,X,R,R,R,R,R\" owns the ten non-SWA layers: layer 2 Full, eight Reuse, and one X at layer 6 that is Reindex-DEFERRED (fb ruling 2026-09-10): Step 2 ships Full/Reuse only, so the module runs X as Reuse until Reindex lands; the slot names the mid-stack selection refresh point rather than a live mode. Global KV = 512 B/token (one Full layer, 4096 B/entry amortized over m=8, bf16) vs 49,152 B/token dense at the same width (12 layers x 2 x d x 2 B) -- 96x smaller. top_k is not effectively dense: 640 keys/query (64x8 + 128) = 15.6% of dense 4096, and the indexer discards 87.5% of the 512 scored entries before attention; indexer cost 131K MACs/query = 3.1% of dense QK. At top_k=128 the attended fraction is 28.1% -- 64 is the default, 128 the A/B ceiling.",
      "measured": "2026-09-10",
      "source": "python3 scripts/v41_size.py (this commit); arithmetic printed by the script, which asserts every constraint above and fails loud if one stops holding. Corrected 2026-09-10 after 3b's changes-requested review on PR #213 (Reuse/Reindex save 2d^2 by reusing Main KV)",
      "config": {
        "paper_values_that_do_not_transfer": "32 indexer heads x 128, 64 core heads x 512, latent 1280, top_k 512, m=2 encoder/1 decoder, 40 layers, n_win 128 -- all 552B / seq 64K-1M (DeepSeek_V41_Tech_Report.pdf section 2)",
        "what_is_borrowed_not_derived": "L=12 was a gap-map suggestion (fb, 2026-09-10; the gap map was a workflow agent's output, not a committed file -- confirmed by fb 2026-09-10 -- so the actual basis is this derivation from the report plus the 352.3M budget); n_win=128 (paper value, Step 3 A/Bs 128 vs 256); MoE 48/top-3/1-shared (pivot ruling, reuse MoEFFN as-is)",
        "active_param_equation": "per layer per Fig. 4: S = 4d^2 + ffn (Q + SWA K/V/O), F = 7d^2 + idx + ffn (adds Main K/V/O), X = 5d^2 + idx + ffn, R = 5d^2 + ffn (Main KV and Top-K reused); idx = 2*d*256 on F/X; ffn = 4*3*d*1728; embedding d*32784 once, tied head. ffn_hidden=6912 is the parity reference only -- all 12 layers are MoE, so it adds no active params",
        "kv_bytes_equation": "dense = LAYERS * 2 * d * 2 B = 49,152 B/token; CSA2 = Full_layers * 2 * d * 2 B / m = 512 B/token; X/R layers store no global KV",
        "density_equation": "attended keys/query = top_k*m + n_win = 640; entries/layer = T/m = 512; selectivity = top_k/entries = 12.5%",
        "ffn_hidden_consequence": "fb ruling 2026-09-10: do NOT relax MoEFFN's equal-active parity check -- set ffn_hidden = (top_k+shared)*expert_ffn = 6912 so the config stays within the check; de wires the value in Step 1",
        "review_correction": "3b's changes-requested review on PR #213 found the first version counted R and X at 7d^2 (Main K/V computed per layer); the correct count is 5d^2 (Main KV reused), an overcount of 2d^2 x 9 reuse-family layers = 18.87M. At expert_ffn=1536 the corrected count was 324.0M, 26M under target; expert_ffn=1728 restores 352.3M. The assertion band tightened from 300-400M to 340-360M so the class is caught next time. (3b's review quoted 321.9M, which assumes 10 overcounted layers; the map has 9.)",
        "composition_with_pr216": "0e-2 (PR #216) puts PureSWA + n_swa_only_layers in HybridLM's kind loop; this derivation's S,S prefix is owned by that field (n_swa_only_layers=2) and csa2_modes owns only the ten non-SWA layers. Open divergence flagged to 0e and de: PR #216's PureSWA reads cfg.csa_window (default 256) while de-103's CSA2 module reads csa2_n_win (128) -- one model must have one SWA width, so PureSWA should read csa2_n_win when the CSA2 module lands.",
        "swa_kv_caveat": "SWA KV is stored uncompressed at the gate (49,152 B/token); bounded replay (Step 7) is what makes the persistent total small, and it is deferred past the gate"
      },
      "uncertainty": "Derivation, not a measurement: the param and byte counts are exact given the assumptions; the assumptions themselves (Q shared, separate KV/O per branch, one Full serving nine Reuse layers, expert_ffn as the active-budget knob) are untested at this scale. The paper runs one Full per ~17 reuse layers at 552B; one per nine here is conservative. m=8 vs m=4 and top_k=64 vs 128 are start values in fb's ranges, not fitted optima. The Reuse/Reindex param count (5d^2) is 3b's correction of the first version's 7d^2.",
      "boundary": "Holds at d=1024, L=12, seq 4096, bf16. FP4 KV, the hierarchical sparse indexer, CED topology and bounded replay are deferred past the gate (pivot); the KV number is the gate-run number, not the deployment number. The gate's ~350M target is met at 352.3M active; if the gap map's L=12 was meant at a different width, the expert_ffn knob changes, not the architecture.",
      "status": "recorded"
    }
  ]
}
