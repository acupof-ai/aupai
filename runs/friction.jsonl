{"when": "2026-09-03 14:31", "who": "de", "kind": "merge", "blocked_what": "de branch merge into main", "cause": "data/pod_head_manifest.txt is regenerated by the pre-commit hook on every commit, so any two branches that both commit conflict on it", "fix_applied": "git checkout -- data/pod_head_manifest.txt then merge again", "minutes_lost": 4, "sha": "c0f8ebd2"}
{"when": "2026-09-03 14:32", "who": "de", "kind": "merge", "blocked_what": "merge main into a branch worktree", "cause": "data/pod_head_manifest.txt is regenerated by the pre-commit hook on every commit, so any two branches that both commit conflict on it", "fix_applied": "git checkout -- data/pod_head_manifest.txt, then merge again", "minutes_lost": 4, "sha": "c0f8ebd2"}
{"when": "2026-09-03 14:32", "who": "de", "kind": "merge", "blocked_what": "merge main into a branch worktree", "cause": "data/pod_head_manifest.txt is regenerated by the pre-commit hook on every commit, so any two branches that both commit conflict on it", "fix_applied": "git checkout -- data/pod_head_manifest.txt, then merge again", "minutes_lost": 4, "sha": "c0f8ebd2"}
{"when": "2026-09-03 14:32", "who": "de", "kind": "merge", "blocked_what": "merge main into a branch worktree", "cause": "data/pod_head_manifest.txt is regenerated by the pre-commit hook on every commit, so any two branches that both commit conflict on it", "fix_applied": "git checkout -- data/pod_head_manifest.txt, then merge again", "minutes_lost": 4, "sha": "c0f8ebd2"}
{"when": "2026-09-03 14:32", "who": "de", "kind": "merge", "blocked_what": "commit on the de branch", "cause": "EXPERIMENTS.md is rendered from runs/experiments.jsonl by exp.py render, so two branches that both render conflict on the rendered file", "fix_applied": "regenerate with exp.py render after the merge instead of resolving by hand", "minutes_lost": 3, "sha": "c0f8ebd2"}
{"when": "2026-09-03 14:32", "who": "6e", "kind": "hook", "blocked_what": "main left ruff-red", "cause": "a fast-forward merge runs no pre-commit hook, so a wip commit that would have been refused on its own branch lands on main unchecked", "fix_applied": "", "minutes_lost": 10, "sha": "c0f8ebd2"}
{"when": "2026-09-03 14:32", "who": "6e", "kind": "check", "blocked_what": "harness task add", "cause": "a hand-written tasks.jsonl row reused an id, and the id allocator takes max+1 over rows matching <owner>-<n> so it cannot see a hand-written duplicate", "fix_applied": "allocate ids only through harness task add", "minutes_lost": 6, "sha": "c0f8ebd2"}
{"when": "2026-09-03 14:32", "who": "de", "kind": "check", "blocked_what": "end-of-run scoring for any new checkpoint on the pod", "cause": "runs/score_matrix.jsonl held two rows for one (ckpt, profile) after a cross-branch union merge, and write_records raises on a duplicate key it does not touch", "fix_applied": "delete the duplicate pod line; the local row was already the merged one", "minutes_lost": 25, "sha": "c0f8ebd2"}
{"when": "2026-09-03 14:32", "who": "98", "kind": "check", "blocked_what": "the 20-minute harness sweep", "cause": "tasks_closed_by_commit and facts_well_formed spawned one subprocess per register row, so their cost grows with the ledger and they time out under hook load", "fix_applied": "batch the git probes: cat-file --batch-check and check-ignore --stdin, 2.15s -> 0.56s and 1.49s -> 0.06s", "minutes_lost": 40, "sha": "c0f8ebd2"}
{"when": "2026-09-03 14:32", "who": "e1", "kind": "launch", "blocked_what": "concurrent probes", "cause": "harness launch routes every non-training job to the single lane card, so two or three probes in one round serialise behind each other", "fix_applied": "", "minutes_lost": 40, "sha": "c0f8ebd2"}
