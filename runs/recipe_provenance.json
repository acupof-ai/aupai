{
  "_comment": [
    "Where each 500M recipe value came from. A value being set is not provenance;",
    "each entry names the artifact that determined it. Written by fb 2026-09-01,",
    "who ruled each one, because nobody else holds the sources.",
    "Values not on this list were not chosen for this run and keep their Cfg defaults. A value PASSED on the command line must be on this list: the launch_command gate reconciles argv against these keys, and a flag with no entry here reads as a knob turned for no recorded reason. warmdown/anneal_frac/warmup/save_every were passed but unlisted until e1-9."
  ],
  "_run": "500M, d1024/L32, 20B tokens, world 8",
  "dim": "1024 — held at today's width deliberately. Instantiating the real HybridLM on a meta device gave 493.6M at L32; three later constructions (tilerl, de) agree. Holding width fixed removes the LR width-transfer axis entirely, which was the largest launch risk (fb ruling, board shape_recipe 2026-09-01).",
  "layers": "32 — the whole parameter increase goes into depth (user order 2026-09-01). d/L 32.0 against the ~45 CompleteP calls compute-optimal at this size: deliberately deeper. Stated to the user as an unsupported bet, not a derivation: 44's survey found no published measurement of depth against free-running agreement in either direction (docs/lessons/trajectory_calibration_survey.md).",
  "heads": "8 — forced, not chosen. FlashKDA's CUTLASS kernel is compiled for head_dim 128, so heads = dim/128. train.py refuses a mismatched pair rather than failing inside the kernel (a52e074).",
  "ffn_hidden": "3072 — 3x width, unchanged from the 200M runs. Width did not move, so this constant did not have to.",
  "batch": "32 — measured. tilerl's compiled batch fit: 32 fits at world=1 (63.95 GiB), 64 does not. The earlier 16 was an EAGER measurement quoted for hours before Cfg.compile was noticed; compile roughly doubles the ceiling in both grad_ckpt arms (probes/t66_depth_shape.py).",
  "accum": "1 -- arithmetic, not a measurement: batch 32 at world 8 gives 8 x 32 x 4096 = 1,048,576 tokens per step (user ruling 2026-09-02, was world 7 / 917,504). The earlier value matched the 200M runs' tok/step exactly so 44's matched-token axis needed no conversion; at world 8 it no longer does and that axis must convert or be abandoned -- the value is unchanged, half its reason is not. The memory alternative b16+accum2 reaches the same tok/step at 36.29 GiB against 69.63, recorded in runs/memory_peaks.json as 7_alt_b16a2, and is reserve for longer seq, not a fallback (b0 caught the stale clause and the untracked citation, 2026-09-02).",
  "lr_scale": "0.85 — b0's t71 measured the depth exponent 12->32 at +0.141 +/- 0.011 over 9 runs, against the 1/sqrt(L) rule's +0.500, so 0.61 was a 2.3x over-correction. Both premises of that rule fail here: AttnRes makes the residual stream a convex combination (sum of softmax weights = 1 at every q scale, so no depth growth to cancel), and Muon normalises away the gradient-magnitude tracking the exponent is derived from. 0.877 is the bound; 0.85 is inside it, chosen because a too-high LR fails loudly in the first 500 steps and a too-low one looks like a healthy run that quietly underperforms (b88de6c, verified by fb running its selftest against the real train.py). The asymmetry is not two symmetric bad outcomes: too high shows up as a loss spike or a gradient-norm blowup in the first 500 steps and is observable; too low produces a curve that looks entirely healthy and simply ends lower than it should, and we have no control run that would reveal it. One failure shouts, the other does not (tilerl).",
  "grad_ckpt": "true — measured, and it inverts with depth. At L=32 checkpointing costs 1.116x per step and gives 4x the batch at LESS peak memory; the 2.4x cost that ruled it out at L=12 was measured against accum 1, which is not the choice at this depth. Stored activations dominate at depth, which is exactly the regime where trading all of them for one recompute wins (eff.grad_ckpt_inverts_with_depth, bc65b15).",
  "warmdown": "0.1 -- the WSD tail, and the one value whose omission is a 6.5x error rather than a nuisance. Cfg defaults to 0.65, which at 19,073 steps starts the cosine descent at step 6,676 = 13.00B tokens instead of step 17,166 = 2.00B (tilerl's arithmetic, docs/lessons/gate_failure_shapes.md shape 28). Launched value read from the register row for this run (runs/experiments.jsonl, p500m_20b_0902, status running).",
  "anneal_frac": "0 -- no anneal phase, matching the stage-1 WSD join of docs/lessons/readout_30b_prereg.md:110 (`--warmdown 0 --anneal_frac 0`). Recorded even though its omission is HARMLESS here: all nine domains of data/mix_500m.json have |weight - anneal| == 0, so the two phases compose identically and a missing flag changes row order only. Listed so the gate can tell 'passed' from 'defaulted' -- which is not the same question as whether it mattered.",
  "warmup": "300 absolute steps, not a fraction: a fractional warmup lost 0.52 val at the 0.2B point (facts/efficiency.json#eff.warmup_absolute_not_fractional). Same 300 as the 30B recipe (docs/lessons/readout_30b_prereg.md:15). Cfg defaults to 20 = 0.105% of this run, and that fallback shipped once on 2026-09-02 without an error.",
  "save_every": "500 steps -- measured, and the key this file was missing until e1-9 (2026-09-02). t38 measured a 959MB checkpoint write at 4.91s median cold, so 500 steps is ~15 min = 0.55% of wall and caps crash loss at one interval; the floor is ~8 min to keep the write under 1% (facts/efficiency.json#eff.ckpt_resume_16h_interval). Cfg/argparse defaults to 1000. Launched value read from runs/experiments.jsonl, p500m_20b_0902.",
  "_not_chosen": {
    "fone": "off — the mechanism is real but the target capability is short executable functions, not arithmetic. e1 runs the A/B in parallel under a three-band rule whose middle band is a reportable 'not decidable at this budget' (f30ca74).",
    "attn_res": "Full, on — user order 2026-09-01, no retest. The A/B was run at 200M; the transfer risk is accepted. attn_res_blocks=8 is the lever in reserve if memory ever forces it, and it is a Cfg value, not a code change.",
    "tokenizer": "not rebuilt — every veto gate passes and the case rested on a compression metric whose documented correlation with downstream reaches -0.09. e1's framing, adopted: rebuilding would be accepting an unmeasured bet, not following the metrics."
  }
}
