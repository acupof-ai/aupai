{"column": "primary", "prompt": "def detect_eucjp(byte_str):\n    \"\"\"Detect if a byte string is likely EUC-JP encoded.\n    \n    Args:\n        byte_str: bytes or bytearray to analyze.\n        \n    Returns:\n        Tuple (state, confidence) where state is one of \"DETECTING\",\n        \"FOUND_IT\", or \"NOT_ME\", and confidence is a float.\n    \"\"\"\n", "answer": "    detector = EUCJPDetector()\n    state = detector.feed(byte_str)\n    return (state, detector.get_confidence())\n"}
{"column": "primary", "prompt": "def count_exclamations(s: str) -> list[int]:\n    \"\"\"Return counts of '!' for each color in the repeating RBYG pattern.\n\n    Args:\n        s: A string containing only 'R', 'B', 'Y', 'G', and '!'.\n\n    Returns:\n        A list of four integers [r_count, b_count, y_count, g_count]\n        where each integer is the number of '!' that should be replaced\n        by that color to restore the correct repeating pattern.\n    \"\"\"\n", "answer": "    n = len(s)\n    colors = 'RBYG'\n    result = []\n    for i, color in enumerate(colors):\n        idx = s.find(color)\n        if idx == -1:\n            phase = i\n        else:\n            phase = idx % 4\n        count = 0\n        pos = phase\n        while pos < n:\n            if s[pos] == '!':\n                count += 1\n            pos += 4\n        result.append(count)\n    return result\n"}
{"column": "primary", "prompt": "def reconstruct(B):\n    \"\"\"Reconstruct array A with minimal sum given B where B[i] = max(A[i], A[i+1]).\n\n    Args:\n        B: A list of integers length N >= 2, guaranteed to be valid.\n\n    Returns:\n        A list A of the same length with minimal sum satisfying the constraints.\n    \"\"\"\n", "answer": "    n = len(B)\n    A = [0] * n\n    A[0] = B[0]\n    A[-1] = B[-1]\n    for i in range(1, n - 1):\n        A[i] = min(B[i - 1], B[i])\n    return A\n"}
{"column": "primary", "prompt": "def utf8_confidence_check(byte_sequence):\n    \"\"\"Return a confidence score for the given byte sequence as valid multi-byte UTF-8.\n\n    Args:\n        byte_sequence: A bytes or bytearray object to analyze.\n\n    Returns:\n        A float confidence value between 0.0 and 1.0. The function counts\n        valid multi-byte UTF-8 characters (lead byte followed by the correct\n        number of continuation bytes). If fewer than 6 such characters are found,\n        confidence = 1.0 - (0.99 * (0.5 ** count)); otherwise confidence = 0.99.\n    \"\"\"\n", "answer": "    data = bytes(byte_sequence)\n    count = 0\n    i = 0\n    n = len(data)\n    while i < n:\n        byte = data[i]\n        if 192 <= byte <= 223:\n            needed = 1\n        elif 224 <= byte <= 239:\n            needed = 2\n        elif 240 <= byte <= 247:\n            needed = 3\n        elif 248 <= byte <= 251:\n            needed = 4\n        elif 252 <= byte <= 253:\n            needed = 5\n        else:\n            i += 1\n            continue\n        if i + needed < n:\n            valid = True\n            for j in range(1, needed + 1):\n                if not 128 <= data[i + j] <= 191:\n                    valid = False\n                    break\n            if valid:\n                count += 1\n                i += needed + 1\n                continue\n        i += 1\n    if count < 6:\n        unlike = 0.99 * 0.5 ** count\n        return 1.0 - unlike\n    return 0.99\n"}
{"column": "primary", "prompt": "def max_increase_keeping_skyline(grid):\n    \"\"\"Return the maximum total increase in building heights without changing skylines.\n\n    For each cell, the height can be raised up to the minimum of the maximum\n    of its row and the maximum of its column. The function returns the sum of\n    all such possible increases.\n\n    Args:\n        grid: A square 2D list of non-negative integers representing building heights.\n\n    Returns:\n        An integer representing the total increase in height.\n    \"\"\"\n", "answer": "    if not grid or not grid[0]:\n        return 0\n    n = len(grid)\n    transposed = list(map(list, zip(*grid)))\n    row_max = [max(row) for row in grid]\n    col_max = [max(row) for row in transposed]\n    total_increase = 0\n    for i in range(n):\n        for j in range(n):\n            allowed = min(row_max[i], col_max[j])\n            increase = max(allowed - grid[i][j], 0)\n            total_increase += increase\n    return total_increase\n"}
{"column": "primary", "prompt": "def find_duplicate(nums):\n    \"\"\"Return the duplicate integer in the input list.\n\n    The list must have length >= 2 and contain integers in [1, len(nums)].\n    Exactly one integer appears more than once, and all other integers appear\n    exactly once. The function uses Floyd's cycle detection algorithm to find\n    the duplicate in O(n) time and O(1) auxiliary space.\n\n    Args:\n        nums: A list of integers satisfying the constraints above.\n\n    Returns:\n        The duplicate integer. If the length is less than 2, returns None.\n    \"\"\"\n", "answer": "    if len(nums) < 2:\n        return None\n    slow = nums[0]\n    fast = nums[0]\n    while True:\n        slow = nums[slow]\n        fast = nums[nums[fast]]\n        if slow == fast:\n            break\n    slow = nums[0]\n    while slow != fast:\n        slow = nums[slow]\n        fast = nums[fast]\n    return slow\n"}
{"column": "primary", "prompt": "def rotate_list_right(nums, k):\n    \"\"\"Return a new list rotated to the right by k positions.\n\n    Args:\n        nums: A list of integers.\n        k: A non-negative integer specifying the rotation amount.\n\n    Returns:\n        A new list with elements rotated right by k positions.\n        The original list is not modified.\n\n    Raises:\n        ValueError: If k is negative.\n    \"\"\"\n", "answer": "    if k < 0:\n        raise ValueError('k must be non-negative')\n    if not nums or k == 0:\n        return nums[:]\n    n = len(nums)\n    effective_k = k % n\n    if effective_k == 0:\n        return [x for x in nums]\n    result = []\n    for i in range(n - effective_k, n):\n        result.append(nums[i])\n    for i in range(n - effective_k):\n        result.append(nums[i])\n    return result\n"}
{"column": "primary", "prompt": "def set_diff_values(ar1, ar2):\n    \"\"\"\n    Return the sorted unique values in ar1 that are not in ar2.\n\n    Parameters\n    ----------\n    ar1 : array_like\n        Input array. Will be flattened if not already 1D.\n    ar2 : array_like\n        Input comparison array. Will be flattened if not already 1D.\n\n    Returns\n    -------\n    ndarray\n        Sorted 1D array of unique values from ar1 that are not in ar2.\n        Returns an empty array if no such values exist.\n    \"\"\"\n", "answer": "    ar1_flat = np.asarray(ar1).ravel()\n    ar2_flat = np.asarray(ar2).ravel()\n    ar1_unique = np.unique(ar1_flat)\n    ar2_unique = np.unique(ar2_flat)\n    if ar2_unique.size == 0:\n        return ar1_unique\n    if ar1_unique.size == 0:\n        return ar1_unique\n    mask = np.isin(ar1_unique, ar2_unique)\n    return ar1_unique[~mask]\n"}
{"column": "primary", "prompt": "def print_star_pattern(rows):\n    \"\"\"Print a diamond-like star pattern with `rows` rows in the upper half.\n\n    Args:\n        rows: A positive integer (or zero/negative to print nothing).\n\n    Prints:\n        A star pattern where each line contains stars equal to the current\n        row number, increasing from 1 to `rows`, then decreasing back to 1.\n    \"\"\"\n", "answer": "    for i in range(1, rows + 1):\n        for _ in range(i):\n            print('*', end='')\n        print()\n    for i in range(rows - 1, 0, -1):\n        for _ in range(i):\n            print('*', end='')\n        print()\n"}
{"column": "primary", "prompt": "def max_sliding_window(nums, k):\n    \"\"\"\n    Return the maximum value in each sliding window of size k.\n\n    Args:\n        nums: List of integers.\n        k: Positive integer window size.\n\n    Returns:\n        List of maximums for each window, or empty list if k is invalid.\n    \"\"\"\n", "answer": "    if not nums or k <= 0 or k > len(nums):\n        return []\n    result = []\n    dq = deque()\n    for i in range(len(nums)):\n        if dq and dq[0] < i - k + 1:\n            dq.popleft()\n        while dq and nums[i] >= nums[dq[-1]]:\n            dq.pop()\n        dq.append(i)\n        if i >= k - 1:\n            result.append(nums[dq[0]])\n    return result\n"}
{"column": "doctest", "prompt": "def min_squares(n):\n    \"\"\"\n    Return the minimum number of perfect squares that sum to n.\n\n    Args:\n        n: A non-negative integer.\n\n    Returns:\n        The minimum count of perfect squares (1, 4, 9, ...) whose sum equals n.\n        For n = 0, returns 0.\n\n    Examples:\n        >>> min_squares(12)\n        3\n        >>> min_squares(13)\n        2\n        >>> min_squares(0)\n        0\n    \"\"\"\n", "answer": "    if n < 0:\n        raise ValueError('n must be non-negative')\n    if n < 4:\n        return n\n    memo = [0] * (n + 1)\n    for i in range(1, 4):\n        memo[i] = i\n    for i in range(4, n + 1):\n        best = i\n        j = 1\n        while j * j <= i:\n            best = min(best, 1 + memo[i - j * j])\n            j += 1\n        memo[i] = best\n    return memo[n]\n"}
{"column": "doctest", "prompt": "def matrix_power(A, power):\n    \"\"\"\n    Raise a square matrix to a non‑negative integer power using recursion.\n\n    This function computes A**power for a square matrix A and a non‑negative\n    integer exponent `power`. It uses the exponentiation by squaring\n    technique to reduce the number of matrix multiplications.\n\n    Args:\n        A (np.ndarray): A square 2D NumPy array of shape (M, M).\n        power (int): Non‑negative integer exponent.\n\n    Returns:\n        np.ndarray: The result matrix A**power, with the same shape and dtype as A.\n\n    Raises:\n        TypeError: If A is not square.\n        ValueError: If `power` is not a non‑negative integer.\n\n    Examples:\n        >>> import numpy as np\n        >>> A = np.array([[0, 1], [1, 0]])\n        >>> matrix_power(A, 2)\n        array([[1, 0],\n               [0, 1]])\n        >>> matrix_power(A, 0)\n        array([[1, 0],\n               [0, 1]])\n        >>> matrix_power(A, 3)\n        array([[0, 1],\n               [1, 0]])\n    \"\"\"\n", "answer": "    A = np.asarray(A)\n    if A.ndim != 2 or A.shape[0] != A.shape[1]:\n        raise TypeError('matrix must be square')\n    n = A.shape[0]\n    if not isinstance(power, (int, np.integer)):\n        raise ValueError('exponent must be an integer')\n    power = int(power)\n    if power < 0:\n        raise ValueError('exponent must be >= 0')\n    if power == 0:\n        return np.eye(n, dtype=A.dtype)\n    if power == 1:\n        return A.copy()\n    half = matrix_power(A, power // 2)\n    if power % 2 == 0:\n        return half @ half\n    else:\n        return A @ half @ half\n"}
{"column": "doctest", "prompt": "def longest_common_prefix(strings):\n    \"\"\"Return the longest common prefix of a list of strings.\n\n    Args:\n        strings: A list of strings.\n\n    Returns:\n        The longest common prefix shared by all strings in the list.\n        Returns an empty string if the list is empty, contains an empty\n        string, or if no common prefix exists.\n\n    Example:\n        >>> longest_common_prefix([\"flower\", \"flow\", \"flight\"])\n        'fl'\n        >>> longest_common_prefix([\"dog\", \"racecar\", \"car\"])\n        ''\n    \"\"\"\n", "answer": "    if not strings:\n        return ''\n    prefix = strings[0]\n    for s in strings[1:]:\n        length = min(len(prefix), len(s))\n        i = 0\n        while i < length and prefix[i] == s[i]:\n            i += 1\n        prefix = prefix[:i]\n        if not prefix:\n            break\n    return prefix\n"}
{"column": "doctest", "prompt": "def subfactorial_values(n):\n    \"\"\"Return a list of subfactorials !0, !1, ..., !n.\n\n    The subfactorial !k counts the number of derangements of k items\n    (permutations with no fixed points). It satisfies:\n        !0 = 1, !1 = 0, and for k > 1: !k = (k-1) * (!(k-1) + !(k-2)).\n\n    Args:\n        n: A non-negative integer specifying the maximum index.\n\n    Returns:\n        A list of length n+1 where the element at index k is !k.\n\n    Raises:\n        ValueError: If n is negative.\n\n    Examples:\n        >>> subfactorial_values(0)\n        [1]\n        >>> subfactorial_values(5)\n        [1, 0, 1, 2, 9, 44]\n    \"\"\"\n", "answer": "    if n < 0:\n        raise ValueError('n must be non-negative')\n    if n == 0:\n        return [1]\n    if n == 1:\n        return [1, 0]\n    seq = [1, 0]\n    for k in range(2, n + 1):\n        next_val = (k - 1) * (seq[-1] + seq[-2])\n        seq.append(next_val)\n    return seq\n"}
{"column": "doctest", "prompt": "def shell_sort(collection):\n    \"\"\"Sort a list in ascending order using Shell sort with Ciura's gap sequence.\n\n    Args:\n        collection: A mutable list of comparable items (e.g., int, float, str).\n\n    Returns:\n        The same list object, sorted in ascending order in-place.\n\n    Examples:\n        >>> shell_sort([0, 5, 3, 2, 2])\n        [0, 2, 2, 3, 5]\n        >>> shell_sort([])\n        []\n        >>> shell_sort([-2, -5, -45])\n        [-45, -5, -2]\n        >>> shell_sort(['banana', 'apple', 'cherry'])\n        ['apple', 'banana', 'cherry']\n    \"\"\"\n", "answer": "    gaps = [701, 301, 132, 57, 23, 10, 4, 1]\n    for gap in gaps:\n        for i in range(gap, len(collection)):\n            temp = collection[i]\n            j = i\n            while j >= gap and collection[j - gap] > temp:\n                collection[j] = collection[j - gap]\n                j -= gap\n            collection[j] = temp\n    return collection\n"}
{"column": "doctest", "prompt": "def unique_everseen(iterable: Iterable[_T], key: Optional[Callable[[_T], _U]]=None) -> Iterator[_T]:\n    \"\"\"Yield unique elements from `iterable`, preserving order.\n\n    If `key` is provided, uniqueness is determined by `key(element)`.\n    Otherwise, uniqueness is determined by the elements themselves.\n\n    Args:\n        iterable: Any iterable of hashable items.\n        key: Optional callable that maps each item to a hashable value.\n\n    Returns:\n        An iterator yielding each distinct element in first-appearance order.\n\n    Examples:\n        >>> list(unique_everseen('AAAABBBCCDAABBB'))\n        ['A', 'B', 'C', 'D']\n        >>> list(unique_everseen('ABBCcAD', str.lower))\n        ['A', 'B', 'C', 'D']\n    \"\"\"\n", "answer": "    seen: Set[Union[_T, _U]] = set()\n    seen_add = seen.add\n    if key is None:\n        for element in iterable:\n            if element not in seen:\n                seen_add(element)\n                yield element\n    else:\n        for element in iterable:\n            k = key(element)\n            if k not in seen:\n                seen_add(k)\n                yield element\n"}
{"column": "doctest", "prompt": "def count_coin_ways(total):\n    \"\"\"Return the number of distinct ways to make change for `total` cents\n    using coins of denominations 1, 5, 10, and 25.\n\n    Args:\n        total: A non-negative integer representing the amount in cents.\n\n    Returns:\n        An integer representing the number of combinations of coins that sum\n        to `total`. The order of coins does not matter.\n\n    Examples:\n        >>> count_coin_ways(0)\n        1\n        >>> count_coin_ways(5)\n        2\n        >>> count_coin_ways(15)\n        6\n        >>> count_coin_ways(100)\n        242\n    \"\"\"\n", "answer": "    def helper(remaining, max_coin):\n        if remaining == 0:\n            return 1\n        if remaining < 0 or max_coin is None:\n            return 0\n        return helper(remaining - max_coin, max_coin) + helper(remaining, next_smaller_coin(max_coin))\n    return helper(total, 25)\n"}
{"column": "doctest", "prompt": "def bit_operations(v):\n    \"\"\"Return popcount, bit length, and set-bit indices for abs(v).\n\n    Args:\n        v: An integer (may be negative or zero).\n\n    Returns:\n        A dict with keys:\n          'popcount': number of 1 bits in binary representation of abs(v)\n          'bit_length': number of bits needed to represent abs(v) in binary (0 for zero)\n          'set_bit_indices': list of indices (LSB=0) where bits are 1\n\n    Example:\n        >>> bit_operations(-5)\n        {'popcount': 2, 'bit_length': 3, 'set_bit_indices': [0, 2]}\n    \"\"\"\n", "answer": "    n = abs(v)\n    if n == 0:\n        return {'popcount': 0, 'bit_length': 0, 'set_bit_indices': []}\n    popcount = 0\n    bit_length = 0\n    indices = []\n    position = 0\n    while n > 0:\n        if n & 1:\n            popcount += 1\n            indices.append(position)\n        n >>= 1\n        bit_length += 1\n        position += 1\n    return {'popcount': popcount, 'bit_length': bit_length, 'set_bit_indices': indices}\n"}
{"column": "doctest", "prompt": "def align_sequences(str1, str2):\n    \"\"\"\n    Compute the optimal alignment of two phonetic sequences.\n\n    Args:\n        str1: First phonetic string (e.g., \"θin\")\n        str2: Second phonetic string (e.g., \"tenwis\")\n\n    Returns:\n        list[tuple[str, str]]: A list of pairs representing the optimal alignment.\n        Each tuple is (char_from_str1, char_from_str2), where \"-\" denotes a gap.\n        Returns an empty list if either input is empty.\n\n    Example:\n        >>> align_sequences(\"kat\", \"kæt\")\n        [('k', 'k'), ('a', 'æ'), ('t', 't')]\n    \"\"\"\n", "answer": "    m, n = (len(str1), len(str2))\n    if m == 0 or n == 0:\n        return []\n    S = [[0.0] * (n + 1) for _ in range(m + 1)]\n    for i in range(1, m + 1):\n        for j in range(1, n + 1):\n            edit1 = S[i - 1][j] + sigma_skip(str1[i - 1])\n            edit2 = S[i][j - 1] + sigma_skip(str2[j - 1])\n            edit3 = S[i - 1][j - 1] + sigma_sub(str1[i - 1], str2[j - 1])\n            if i > 1:\n                edit4 = S[i - 2][j - 1] + sigma_exp(str2[j - 1], str1[i - 2:i])\n            else:\n                edit4 = float('-inf')\n            if j > 1:\n                edit5 = S[i - 1][j - 2] + sigma_exp(str1[i - 1], str2[j - 2:j])\n            else:\n                edit5 = float('-inf')\n            S[i][j] = max(edit1, edit2, edit3, edit4, edit5, 0)\n    max_score = max((max(row) for row in S))\n    if max_score == 0:\n        return []\n    max_i, max_j = (0, 0)\n    for i in range(m + 1):\n        for j in range(n + 1):\n            if S[i][j] == max_score:\n                max_i, max_j = (i, j)\n                break\n        else:\n            continue\n        break\n    alignment = []\n    i, j = (max_i, max_j)\n    while i > 0 and j > 0 and (S[i][j] > 0):\n        if i > 1 and S[i - 2][j - 1] + sigma_exp(str2[j - 1], str1[i - 2:i]) == S[i][j]:\n            alignment.insert(0, (str1[i - 2:i], str2[j - 1]))\n            i -= 2\n            j -= 1\n        elif j > 1 and S[i - 1][j - 2] + sigma_exp(str1[i - 1], str2[j - 2:j]) == S[i][j]:\n            alignment.insert(0, (str1[i - 1], str2[j - 2:j]))\n            i -= 1\n            j -= 2\n        elif S[i][j - 1] + sigma_skip(str2[j - 1]) == S[i][j]:\n            alignment.insert(0, ('-', str2[j - 1]))\n            j -= 1\n        elif S[i - 1][j] + sigma_skip(str1[i - 1]) == S[i][j]:\n            alignment.insert(0, (str1[i - 1], '-'))\n            i -= 1\n        elif S[i - 1][j - 1] + sigma_sub(str1[i - 1], str2[j - 1]) == S[i][j]:\n            alignment.insert(0, (str1[i - 1], str2[j - 1]))\n            i -= 1\n            j -= 1\n        else:\n            break\n    return alignment\n"}
{"column": "doctest", "prompt": "def nth_lexicographic_permutation(digits, n):\n    \"\"\"Return the n-th lexicographic permutation of a list of distinct integers.\n\n    Args:\n        digits: A list of distinct integers (any order, will be sorted internally).\n        n: A positive integer, 1-indexed, representing the permutation position.\n\n    Returns:\n        A list of integers representing the n-th permutation in sorted order.\n\n    Example:\n        >>> nth_lexicographic_permutation([0, 1, 2], 4)\n        [1, 2, 0]\n    \"\"\"\n", "answer": "    if not digits:\n        return []\n    sorted_digits = sorted(digits)\n    k = len(sorted_digits)\n    n -= 1\n    factoradic = []\n    for i in range(1, k + 1):\n        n, remainder = divmod(n, i)\n        factoradic.append(remainder)\n    factoradic.reverse()\n    result = []\n    available = sorted_digits[:]\n    for digit in factoradic:\n        result.append(available.pop(digit))\n    return result\n"}
