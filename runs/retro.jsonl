{"owner": "3b", "date": "2026-08-31", "incidents": [
  {"when": "clean runs", "evidence": "runs/clean_code_rp1t*.log", "cause": "a fetch or clean dies silently and the next step sees only the empty log"},
  {"when": "curl-92 miscount", "evidence": "datagen/fetch_corpus.py _fetch_one", "cause": "HTTP/2 stream error exits nonzero through curl --retry and killed the run unless curl is non-fatal with old : .part resume"},
  {"when": "--filters mismatch", "evidence": "runs/en_stage2_global.log REFUSE en_c4 code-family", "cause": "default --filters web rejects an English/code family ~100%, so the launch must name --filters light"},
  {"when": "shard rollover", "evidence": "datagen/build_corpus.py ShardWriter (fix 75bef8d)", "cause": "worker count advanced after opening the next index, so close-rename hit the wrong name"},
  {"when": "duplicate writers", "evidence": "stale w*_*.part guard + host/container PID split", "cause": "two writers on one domain race the same .part: now refused by the stale-shard guard"},
  {"when": "wrapper death read as process death", "evidence": "kill container PID only reaped the wrapper; setsid python kept running", "cause": "host PID and container PID diverge, so a kill that targets the wrong side reports dead while the process walks the corpus"},
  {"when": "hand-written stamps", "evidence": "t44 stamps fingerprint UNKNOWN; t31 died at the check", "cause": "a stamp written by hand carries no content fingerprint, so the mix gate refuses it"},
  {"when": "reports disagree with ls", "evidence": "en_c4_stage2 checks from wrong cwd", "cause": "inspection ran relative paths from a non-/work cwd and reported missing what absolute-path ls shows present"}
], "change_landed": ["t37 mirror failover + --selftest; give-up names the chain", "t44 four zh domains re-stamped through corpus_fingerprint with real fingerprints", "parallel exact count (COUNT_WORKERS) as the t21 cross-check"], "change_proposed": "build_corpus pre-flight: refuse unless unique writer by host pgrep, output dir present and not a ladder-frozen name, filter family matches domain, and a 1000-doc sample passes end-to-end; each with a failing-case test", "differently": "use absolute paths in every pod inspection; kill and verify by host PID; never hand-write a fingerprint; state --filters explicitly; read the log before and after a process exit"}