# Controller board (fb) — updated 2026-09-04T21:04Z, rewritten every tick

Percent = share of the deliverable landed on main and verified by a second reader; "quality" names what the reviewer opened, or the defect the owner caught in their own work.

## Memory layers program (user order 2026-09-05; charter docs/standards/memory_layers_0905.md, prereg #memory_layers_0905 + amendments 1-6 + readout_5_result_m1)

| item | owner | % | delivered | quality / evidence | next gate |
|---|---|---|---|---|---|
| Memory layer in model.py + train.py wiring | b0 | 95 | model on main; Adagrad group, FP8/Muon exclusion by fqn (4d0319cf), MasterWeights exclusion + pull_grads half (dc5c1a79), memory_diag wired with real Gini/entropy and global all_reduce, try/except around the write (4c71360a); dense params == control exactly, in the prereg row (25294f57); M1 smoke: 68.46 GiB, ratio 0.803 (>= 0.70) | §177 attn_res; 12 bytes/param finding (M3 2048² OOM); pull_grads would have silently frozen the table; 44 review e20ae279; tilerl review of FP8 by running the real filter | M1 launched twice and STOPPED twice: 19:41 world-6 (grant named 6 block cards, launcher took them all, §188; row closed error), 19:47 on cards 1+2 stopped at step 83 by readout 5 (64K@30, 65K steady vs 82K control = ratio 0.78 < 0.85; row closed stop). Close-out landed: runs/b0_mem_m1_vs_control_tps.json (3714bd92), MFU uses n_dense and reproduces the control's 34% exactly (167% with the table was the tell), claim already released, interrupt ckpt untouched. Ledger: 19:55 row is a phantom identity (exp.py done without --started); ruled append-only, note in prereg amendment_6. NEW BLOCKER (b0 finding): table is bf16 with no master copy; readout 4 blind to a frozen table; provisional fix fp32 master (12 B/param), readout 6 rows_changed added (amendment_7). b0_mem_m3_peak done (af34cb17): bf16 confirmed, 88.15 GiB reserved at 1448²; fix LANDED on main 1928e13e (TableMaster + readout 6 checksum + known-answer selftest, 6 mutants killed; pod push HELD until tilerl's cells finish so the decomposition stays comparable); 16 B/param in-step peak, 14 steady, master.grad early-free would only remove the 16 moment (deferred, measured 4 B/param); M3 resized to 1195 (amendment_8); 1195 one-layer baseline WITHOUT master: 63.48 GiB reserved, gap 0.33 (expandable_segments), non-table 55.31, READOUT6 9.15 ms = 0.47% of step; projected with master 77.1 GiB. Probe had no TableMaster (instrument omitted the fix; fixed 440bd579, pushed); re-run with master on lane 5 NOW; tilerl master-on/off pair on 1+2 NOW. Relaunch after: TABLE line, master fix + diag field in one announced commit, tilerl decomposition >= 0.85 |
| Kernel/throughput + gradient exchange + module boundary | tilerl | 100 | item 1 lookup 93.9% of control; item 2 sparse exchange loses at every arm → dense ruled; item 3 compile 0 breaks on the pod, FP8 conversion table (table never reachable, projections bf16 by ruling); probes committed; charter amended | reversed own prediction; two tooling defects (§181) | RUNNING on cards 1+2 (claim tilerl_mem_decomp_off.1-2.json, first cell compiling): five cells off/m1/k16/l1/m2 at the arm launch line, world 2, expandable_segments; m2 cell reads M2's ratio directly for readout 5. Cells off/m1 in: 82,300 vs 64,987 tok/s/gpu (reproduces the arm); M1 costs +424 ms/step = fwd +90 (lookup 51.6, bookkeeping ~38), bwd +315 (74%, the anomaly: 3.5x its forward), opt +19 (Adagrad ~1.2 points). Not DDP: world 1 and 2 agree. Cells k16 (69,391) and l1 (74,334 = 0.90) in: fit 3a+b gives per-layer 29.9/97.3/-0.1 ms and table-fixed 0.1/23.3/19.2 ms (fwd/bwd/opt), so 382 of 424 ms is per-layer and the backward scatter-add scales with top_k (atomics-bound, ~25 ns per row-gradient). m2 0.81 (cost is not the table); gradient bench: index_add and sort+segment both 2.3x SLOWER than the dense backward, a free scatter reaches only 0.841. RULED (amendment_10): arms relaunch with ONE pooled layer at layer 6 (measured 0.90); 0.85 gate unchanged. nobk cell pending, cannot change the verdict. Claim fixed per cell (a788bd0b). Then M3 exchange at the resized side |
| Readout 2 fact probe (seen-vs-unseen DiD, API-name cloze) | e1 | 70 | first item file VOID (contiguous-prefix boundary wrong; plan shuffled + rank-striped, §179); corrected: SEEN = replayed read set (union over ranks, 80,280 rows), UNSEEN = never-allocated tail [643969, 2139719); eval/api_cloze.py refuses headerless files; cluster-robust SE beside naive (b8f308dd); rebuild running on the pod | own refutation was in hand (21.07 rows/step) and explained away; 3b reproduced the replay independently and retracted its own row | rebuilt file committed with n_seen_rows = 80,280 + provenance; 3b's 50/50 sample; control floor on lane 5 (lane free now) |
| Data parity + region review + launch-time UNSEEN guard | 3b | 95 | 9/9 fps; zh_web confirmed exactly; independent replay (nine cursors); launch_gate refuses an arm reading into the UNSEEN region (6feb6708, 1f47d4ed); cluster_se reviewed on five known answers | retracted own contiguity row with the right diagnosis; §178 | review the rebuilt item file (header 80,280; 50 SEEN / 50 UNSEEN) |
| Prereg row + amendments | fb | 100 | row 30f3f29b; amendments bcc86d18, 87960318, 6fe808a5, wording 25e0ca58 | 44 review 8319002d: dates PASS, superseded-marker fixed, stale charter sha fixed | — |
| memory_diag ledger + monitors + fixture gate | de | 90 | ledger + schema + memory_diag_fresh (95579a06); monitors with stop rules from one owner, never kill (7134053d); schema added to pod push SCOPE after it was found missing on the pod (14b9a6a1, caught before launch); fixture-only-checks gate 7c3a2d29 | arm-name matcher that matched its own fixture and no real arm; two blind mutants | option B landed (624260f4) and M1 launched through it. card_claim fix LANDED 121a865d (device-fd predicate, blocking 90 s poll, verified on the pod: 1.18 s to a pid holding 31 device fds; wrapper shell refused); SWEEPS UNFROZEN. Limit stated: a job whose shell exits before the claim reparents to init and gets no claim (W3). §196 false-defect in test_launch_claims fixed. Next: --cards subset + refuse on CVD disagreement in harness launch; then (2b) owned output dirs, `harness ruling`; §186 §187 |
| score_matrix_present sees #cu rows | de | 100 | 00:56 commit | found by b0 on armB | — |
| Progress page + experiments doc section | 98 | 100 | 67b2ac04, 2040f009 | follows amended prereg, no unledgered numbers | page says M1 stopped (throughput rule), decomposition next (c8b09b15); curves when a run passes step 30 |
| Card grant | fb | 100 | all 8 cards ours; grant bf648f93 names ONE arm's cards (block_cards "1,2", now lent to tilerl for the decomposition); 4,6 reserved M2; 0,3 reserved M3; lanes 5,7. Lesson §188: harness launch takes the grant's whole block, so a grant must name one arm | pod at tick: all cards 0 MiB, runs/claims empty, 3 zombies (842205/842276/842277) unreaped | re-grant "4,6" for M2 only on a predicted ratio >= 0.85; "0,3" for M3 after its --peak-only |

## Global items carried from 2026-09-04

| item | owner | % | state | next |
|---|---|---|---|---|
| CI green | de | 100 | four consecutive greens (bb876bd7, 27b9ea15, bd82cad6, 95579a06) | 25e0ca58 and 10acde74 green; 6fe808a5 in progress; four reds fixed (launch_tests, score_matrix_failpath, holdout regenerate, pod_drift world, free-card fixture) | CI-red WARN 2 h / FAIL 8 h check still owed |
| Harness: pod-reaching checks time out when tunnel down | de | 60 | UnboundLocalError in strike handling fixed (8319002d); reachability SKIP written, not committed | deadlines raised (stopgap); pod_stamp_is_main → auth=pod with reachability SKIP written, not committed | commit with the dead-host broken world |
| Harness package split (core/checks/cmds/infra) | de | 0 | design accepted | plan doc after the memory program's monitors |
| Funnel table (T0-T3 per rule) + gates_inventory.tsv | de | 0 | tiers ruled | table |
| temp-world leak (12 dirs / 7.5 MB per harness selftest; 22 dirs / 12 MB per test_exp_done_started) | de | 100 | scripts/tmpworld.py process-level redirect + rmtree at exit, four call sites, 4 → 0 leaked through the hook (bd82cad6); 349 MB freed | mutation-tested 4 ways; test_attest_cases.py still leaks by hand (not in SELFTEST_FILES) |
| Infra decoupling (aupai-infra) | tilerl | 40 | inventory + contract 326831f1; wrappers vendored 1ab666f6 with cd-into-background refusal; two inventory misfiles returned | subtree cut after the memory program's kernel work |
| Prose rules → layers (user direction 2026-09-05: less control, brief before the work) | 44+de | 50 | 31 manual rules reviewed against 5 sources: KEEP 15 / BRIEF 14 / MOVE 2 / DELETE 0 (ec64087f, 3d11225f); rule→kind table handed to de; `harness brief <kind>` + two L1 gates (fixture-only checks; owned output dirs) + `harness ruling` queued at de behind the monitors | Shared-files T0 (3b, file_claim + owner scoping ed3eba36), Language T3 deleted, rule 1 (e1), rule 2 T1 (e1 38af3d47), rule 3 (e1, unblocked, after the probe) | rule 3 train.py ETA |
| gate_failure_shapes restructure + 33 folded incidents | 44 | 100 | 141 = 59 + 82; 33/33 located, §162 re-cited, file::function rule; §175 §176 landed | §177 (attn_res) on b0's write-up |
| Friction summary + fixes | 44 | 100 | 2fa5c710; post-merge hook warn-only; minutes_required check | daily |
| Credential in transcripts (ANTHROPIC_AUTH_TOKEN) | fb | 100 | 83 occurrences in 6 transcripts + 2 zsh_history lines redacted; repo/pod clean across 18 branch tips and 4149 commits | user rotates the relay token; GitHub secret scanning is off on the repo (user setting) |
| Laptop disk | user | 100 | 5 GB → 112 GB free | /private/tmp/e1_v11_snapshot (2.8 GB, 8 projects' transcripts) left in place per user |
| Head-hybrid A/B | b0 | 100 | B loses 0.087 nat; closes + prereg outcome 15de1e0e; checkpoints pinned | user: keep or prune both arm checkpoints |

## Open user decisions (unchanged)
/data01 backup refresh schedule; code_tests fetch placement on /data02; head-hybrid and Stage E checkpoints keep/prune; e1_v11_snapshot; rotate ANTHROPIC_AUTH_TOKEN in ~/.zshrc (11 lines still hold it); web_cci3_p*/batch_*.jsonl and M1 interrupt checkpoints left in place (no deletion without a named order).

## Rulings this night (all in prereg amendments 6-9)
Readout 5 same-step ratio < 0.85. Table is bf16 with no master (readout 4 blind to a frozen table) -> table-owned fp32 master, 16 B/param peak; readout 6 per-row checksum. M3 1448² does not fit with the fix -> 1195 pending a probe with the fix; all arms expandable_segments. b0_mem_m1 has three ledger identities, ruled append-only. Defects queued at de: poll-then-claim on the torchrun pid; pod wrapper `cd … & ; …` hole.

## Rules restated this tick
Readout 5 is a same-step ratio arm/control, stop < 0.85 (5928a179); the 70K absolute floor was wrong because control itself reads 56K by step 200. Sweeps and reclaims are frozen on the pod until the card_claim fix lands.
